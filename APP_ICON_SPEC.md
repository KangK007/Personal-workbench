# 应用图标规范 ·「新芽 · 破土」

> **单一真源 = `tool/build_app_icon.py`。**
> 改配色或造型，只改该文件顶部的 `PALETTE` 与几何常量，重跑
> `python tool/build_app_icon.py` 即全链路同步：品牌母版 → Windows ICO →
> Android 三份矢量 → 全尺寸样张 → 预览页，一次重出。
> 预览：`design_preview/icon/index.html`

---

## 1. 用途与主题

| 项 | 内容 |
|---|---|
| 用途 | 「个人工作台」的应用图标：Windows 快捷方式 / 任务栏 / 开始菜单 / Alt-Tab、Android 启动器（传统档 + 自适应档） |
| 主题 | **新芽破土**——一株白芽从陶土色的土地里长出来，上面是新芽绿的天空 |
| 与设计系统的关系 | 取自同一套语义：**新芽绿 = 生长**（`lightPrimary`）、**陶土 = 土地与成果**（`lightReward`）。图标不是另起一套配色，而是把设计系统的两枚主轴色放到同一张画布上 |
| 与界内品牌标记的关系 | 与 `SealLogo`（侧栏品牌块）**同源不同用**：共用 0.22 圆角比与同一色板，但图形不同——见 §12 |

---

## 2. 风格定位

**扁平实色 + 有机曲线。** 无渐变、无阴影、无高光、无玻璃拟态、无描边式线条画。

排除其它风格的理由，逐条写下来是为了避免下一轮又「顺手换个风格」：

| 排除 | 理由 |
|---|---|
| **线条画** | 线条在 16px 下会退化成 ≤1px 的灰线，失去重量与辨识；且与界面里「面板 = 实色 + 1px 中性边框」的纪律相反——图标不是线框图 |
| **拟物** | 与整个设计系统冲突。系统明确禁用装饰渐变与玻璃拟态，拟物图标会是全应用唯一一处「假的材质」 |
| **渐变** | 同上。另外渐变在 16px 下只有几个色阶，看不出渐变，只剩脏色 |
| **多色插画** | 16px 只有 256 个像素，超过 3 色就开始互相吃辨识度 |

**形状语言**：叶片用三次贝塞尔构成的两条不对称叶缘（外缘饱满、内缘平缓），叶尖保留约 70° 的柔角——不是锐角尖刀，也不是圆头汤匙。芽自茎体内生出，所以从茎到叶的结合处没有缺口。

---

## 3. 配色

全部取自 `lib/core/theme/app_theme.dart` 现行令牌，**不引入新色值**。

| 主题 | 层 | 色值 | 令牌 | 芽在其上（WCAG 实算） |
|---|---|---|---|---|
| 浅色 | 天空 | `#3E7F4B` | `lightPrimary` 新芽绿 | **4.84:1** |
| 浅色 | 大地 | `#9B5227` | `lightReward` 陶土 | **5.79:1** |
| 浅色 | 芽 | `#FFFFFF` | `lightSurface` | — |
| 深色 | 天空 | `#16231A` | `darkSurface` | **8.40:1** |
| 深色 | 大地 | `#4A3629` | `darkRewardContainer` | **5.85:1** |
| 深色 | 芽 | `#7FCB8E` | `darkPrimary` | — |

**天空与大地为什么用 ΔE₀₀ 而不是对比度来判断。**
这是一条容易被误判的判据。二者亮度接近（对比度仅 1.20:1 / 1.44:1），若拿对比度当尺子会
判定为「不可辨」——但对比度只衡量**亮度差**，而两色是**色相差**（绿 vs 陶土）。
按本项目既定判据改用 CIELAB ΔE₀₀：**浅色 42.4 / 深色 21.6**，均远超「显著」门槛（>20）。
同时必须说明：这条分界是**装饰性**的，不承载任何必需信息，所以即便在灰度或
极端色觉条件下它消失，也不构成可达性缺陷——芽本身在两种底色上都 ≥4.5:1，信息完整。

**砖底为什么要能同时压住浅色和深色任务栏。**
`#3E7F4B` 对浅色外壳 `#F3F3F3` 为 **4.36:1**、对深色外壳 `#1F1F1F` 为 **3.41:1**，两端都过 3:1
（图形元素门槛）。所以 **Windows 一律用浅色档**，不必随系统主题切换图标。
反观深色档：它的砖底对深色任务栏只有 1.01:1——**深色档不可用于深色外壳**，
仅限应用内深色画布等自身可控的场合。

---

## 4. 几何

| 项 | 值 | 说明 |
|---|---|---|
| 设计网格 | 1024 × 1024 | 全部坐标以此为准；栅格化时先超采样到 2048/4096 再逐级降采样 |
| 圆角砖 | `r = 232` | ≈22.7%，与界内 `SealLogo` 的 0.22 同源 |
| 芽包围盒 | `y ∈ [218, 884]` | 占砖高 **65%**，上方留白 21% |
| 茎宽 | `82` | 在 40px 档为 **3.05px** |
| 茎露出 / 埋入 | `316` / `116` | 露出部分即肉眼所见的整株；埋入只为制造「长出来」的关系 |
| 叶轴 | `268 @ 47°` | 与竖直方向夹角 |
| 叶全宽 | `108 × 1.30` | 在 40px 档为 **8.04px** |
| 大地弧 | `R = 1639.4` | 顶点 `y=768`、两端 `y=850`、半弦 512（正好触到砖左右边缘） |
| 弧的半径/弦长比 | `1.6010` | 传统档与自适应档**必须相同**——否则两端图标不是同一个东西（已入自检） |

**「破土」是怎么做出来的**：渲染顺序是 天空 → 芽 → 大地。茎的绘制终点在 `y=884`，
深埋在大地顶点 `y=768` 之下 116 单位处，然后被大地覆盖。所以芽看起来是从土里长出来的，
而不是插在土上。自适应档里两层分开，前景的茎在 `y=768` 处**被截断**，与背景弧的顶点对齐
（自检实测两个值都是 `69.19`，误差 < 0.05）。

---

## 5. 尺寸分档与多分辨率适配

**分档线：40px。** ≥40px 用全形态，<40px 用简化档。

| 档 | 特征 |
|---|---|
| **全形态** | 芽包围盒 `y ∈ [218, 884]`，茎宽 82，叶半宽 108 |
| **简化档**（<40px） | 芽抬升放大到 `y ∈ [240, 884]`，茎宽 ×1.19、叶半宽 ×1.12 |

简化档只做一件事：**让小尺寸下的芽仍是可辨的整体轮廓**。16px 时全形态的叶尖只有不到 1px，
细部会碎成抗锯齿灰点；加粗放大后轮廓反而更明确。
天空/大地在两档里都保留——它们是大色块，天然抗缩小。

**Windows ICO 不是「一张图缩七次」。**
`app_icon.ico` 含 **16 / 24 / 32 / 48 / 64 / 128 / 256** 七档，每档由几何**独立渲染**，
且 <40px 的 16/24/32 三档走简化档。改造前该文件只含 256 单档，任务栏与快捷方式的
16px 全靠系统缩放——那才是图标糊掉的真正原因。

**Android 传统启动器**：`mdpi 48 / hdpi 72 / xhdpi 96 / xxhdpi 144 / xxxhdpi 192`，
全部 ≥40px 故走全形态。

---

## 6. 深色主题与 Android 13+ 单色层

**深色档**（`app_icon_dark.*`）：与浅色档**同几何、同结构**，只换色。
深色天空 `#16231A` + 深色土地 `#4A3629` + 浅绿芽 `#7FCB8E`。
用途限于应用内深色画布、深色主题的文档封面等——**不要拿它当 Windows 图标**（见 §3）。

**Android 13+ 主题单色层**（`drawable/ic_launcher_monochrome.xml`）：
与前景同几何的纯白剪影，由系统着色。用户开启「主题图标」后，系统会把整枚图标
收成单色并跟随壁纸取色——此时天空与大地的两分法消失，只剩芽的剪影，
所以单色层里**只保留芽**，不保留底色。这一点是刻意的：单色层是剪影，不是缩小的彩图。

---

## 7. 遮罩适配

Android 自适应图标由系统决定外形，所以两层都必须能承受任意裁切。

| 层 | 内容 | 约束 |
|---|---|---|
| `ic_launcher_background.xml` | 天空铺满 + 大地弧 | 全出血，弧半径按**画布比例**取 |
| `ic_launcher_foreground.xml` | 芽 | 整体缩入安全圆 |
| `ic_launcher_monochrome.xml` | 芽（纯白剪影） | 同上 |

**安全圆取 62/108，而不是官方保证的 66/108。**
官方保证中央 66/108 圆内内容不会被裁，但芽的**叶尖正好是包围盒的角点**——
按 66 取，叶尖会精确落在安全圆上，圆形遮罩下贴边；取 62 留 6% 余量。
自检实测包围圆半径 **31.00 / 54**（上限 31.5）。

**一个必须记下来的坑：背景弧的半径不能跟着芽的缩放一起缩。**
最初的实现里背景与前景共用同一个缩放系数，结果遮罩窗口内的地平线比传统档明显更弯——
两个平台的图标看起来不是同一个东西。正确做法是：**弧半径按画布比例取（与传统档同弧度，
半径/弦长比 1.6010），只有弧的顶点高度跟随芽的落位。** 这条已写成自检项防回归。

---

## 8. 落地位置

| 产物 | 位置 | 形态 |
|---|---|---|
| Windows 图标 | `windows/runner/resources/app_icon.ico` | 7 档 DIB 条目，363.8 KB |
| Android 传统启动器 | `android/app/src/main/res/mipmap-*/ic_launcher.png` | 48 / 72 / 96 / 144 / 192 |
| Android 自适应背景 | `.../res/drawable/ic_launcher_background.xml` | VectorDrawable |
| Android 自适应前景 | `.../res/drawable/ic_launcher_foreground.xml` | VectorDrawable |
| Android 13+ 单色层 | `.../res/drawable/ic_launcher_monochrome.xml` | VectorDrawable |
| 自适应装配 | `.../res/mipmap-anydpi-v26/ic_launcher{,_round}.xml` | 指向上面三份 |
| 品牌母版（矢量） | `assets/branding/app_icon_source{,_dark}.svg` | 1024，改稿起点 |
| 品牌位图 | `assets/branding/app_icon{,_dark}.png` | 1024 × 1024 |
| 预览页 | `design_preview/icon/index.html` + 19 张样张 | 随生成器重出 |

由 Windows 资源编译取用：`windows/runner/Runner.rc` → `IDI_APP_ICON ICON "resources\\app_icon.ico"`（未改动，路径已就位）。
`android/app/src/main/AndroidManifest.xml` 的 `android:icon="@mipmap/ic_launcher"` 亦未改动。

---

## 9. 改动流程

```bash
# 1) 改 tool/build_app_icon.py 顶部的 PALETTE 或几何常量
# 2) 重跑（自检会先跑，全绿再落盘）
python tool/build_app_icon.py
# 3) 看预览页确认观感
#    design_preview/icon/index.html
```

自检（每次运行先跑，失败会阻断）共 16 项，覆盖：

- 芽在两档下**越出圆角砖的像素数必须为 0**
- 自适应前景包围圆半径 ≤ 31.5（官方保证 33）
- 茎的埋入量与露出量（露出须 >240）
- **自适应两层对齐**（前景茎截断点 == 背景弧顶点，误差 <0.05）
- **弧半径/弦长比一致**（传统档 == 自适应档）
- 茎宽、叶全宽在 40px 档须 ≥1.5px
- 6 组对比度 + 2 组 ΔE₀₀ + 砖底对浅/深外壳

配色判据复用 `tool/verify_colors.py` 的 `contrast` / `delta_e00`
（该文件已加 `__main__` 守卫，可安全 import，避免 ΔE₀₀ 双实现）。

---

## 10. 本轮同时修掉的既有缺陷

图标本身之外，顺着打包链路查出四处同类问题，全部属于「上一代配色/素材没清干净」：

| # | 位置 | 问题 | 处置 |
|---|---|---|---|
| 1 | `windows/runner/resources/app_icon.ico` | **只含 256 单档**，16px 靠系统缩放 → 任务栏、快捷方式糊 | 重出为 7 档逐档渲染 |
| 2 | `res/values/ic_launcher_colors.xml` | 底色 `#2F7D57`，是**两代前的废弃冷绿**，且是全仓最后一处 | 改为矢量背景；该文件已无引用，删除 |
| 3 | `res/drawable/ic_launcher_foreground.xml` | 三个**同心圆**，与品牌毫无关系，还被同一条目兼作 Android 13+ 单色层 | 换成芽；单色层拆为独立文件 |
| 4 | Android 启动链路取色 | `launch_background.xml`（浅 `#F6F8F6` / 深 `#0E1311`）、`values/styles.xml`（`#F2F7F3` + `#FBFDFB`）、`values-night/styles.xml`（`#0F1712` + `#16231B`）——**三个互不一致的「浅色底」**，注释与取值还互相打架 | 9 处统一到 `lightCanvas` / `darkCanvas` / `lightSurface`，注释改为指向令牌名 |
| 5 | `assets/branding/app_icon.png` vs `app_icon_source.svg` | 位图是青色「清单 + 对勾」，矢量是深底「三竖 + 金勾」——**两者互不一致**，且都不是界内的品牌标记 | 一并替换为本轮设计 |

---

## 11. 验收

| 项 | 结果 |
|---|---|
| 生成器自检 | **16 / 16 通过** |
| Android XML 良构性 | 11 个 XML 全部可解析 |
| VectorDrawable 指令 | 仅用 `M / L / C / A / H / V / Z`，无非法指令 |
| ICO 条目回读 | 7 档全部可解码，四角透明，大地实测 `#9B5227` |
| 废弃色残留 | `android/` 下已归零 |
| 预览页 | 27 处引用全部存在，无孤儿样张，标签全配对 |

---

## 12. 未纳入本轮 / 后续建议

**① 界内品牌标记 `SealLogo` 与本图标仍未统一。**
`SealLogo`（`lib/ui/widgets/ink_decoration.dart`）是「绿色圆角砖 + 白色页面卡 + 折线路径 + 琥珀节点」，
与本图标的「芽」是两套图形。它们共用色板与 0.22 圆角比，但**形态不同源**。
本轮刻意没动它：改它会影响 `workbench_shell.dart` / `app.dart` 的侧栏与启动页，
并连带 golden 重录，属于独立的形态改版，不宜混在图标交付里。
**建议**：若要收敛，把 `SealLogo` 改为本图标的芽 + 大地弧（约 20 行 `CustomPainter` 改动，
外加一次 golden 重录），届时全应用只剩一个品牌符号。

**② `tool/generate_user_manual.py` 仍在用整片旧色板。**
第 21–26 行：`GREEN = 2F7D57` / `DARK_GREEN = 1F4D3A` / `INK = 1B2B20` / `MUTED = 617268` /
`TABLE_FILL = E8F2EC` / `BORDER = B9CEC0`——这是**上一代**的整套配色，会直接印进用户手册 .docx。
建议按角色机械映射一次：
`GREEN→3E7F4B`、`DARK_GREEN→1D4A27`、`INK→17301B`、`MUTED→5D7961`、
`TABLE_FILL→E2F3E5`、`BORDER→C4D8C9`，然后重出手册并核验。
本轮未动：它属于另一类交付物（文档），需要单独的出图核验，混进来会稀释这次图标的验收边界。

**③ `pubspec.yaml` 未声明 `assets/branding/`。**
`assets/branding/app_icon.png` 目前**没有**被打进应用包——界内用的是代码绘制的
`SealLogo`，图标只供打包链路与文档使用。这是有意的（不打包无用资源），
但若将来界面要直接显示这枚图标，记得先加资源声明。
