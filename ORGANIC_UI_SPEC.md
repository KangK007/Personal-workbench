# 柔壤 · 年轮 — 个人工作台 UI 设计方案 v1.0

> 设计系统代号 **「柔壤 · 年轮」**。风格方向：温柔有机（柔和自然色系、圆润有机线条、充足留白）。
> 本文件是 Flutter 实现与 `design_preview/organic/index.html` 的共同设计源，**取代**根目录 `MASTER.md` 的视觉章节。
> 产品语义、路由、控制器调用、数据模型与真实文案不因本次视觉调整而改变。
>
> 配色对比度为**实测值**（WCAG 2.1 相对亮度公式），非估算。核算脚本逻辑见第 12.4 节。
>
> **2026-09-19 换代（方案 C · 绿）**：色板由「晨间陶土 / 夜间炭壤」切换为
> **「新芽晨光 / 夜露」**。第 4 节为**现行色板**；主行动色语义名由 `sprout` 改为
> `sprout`。第 13、15–18 节是历史记录，保留旧名与旧色值，不作修订。

---

## 1. 设计定位：先看用户怎么用，再决定长什么样

### 1.1 目标用户的真实操作习惯

| 场景 | 环境 | 行为特征 | 对界面的隐含要求 |
| --- | --- | --- | --- |
| 周中晚上规划 | Windows，桌面，2 小时连续 | 多窗口来回切、键盘为主、长时间阅读任务与笔记 | 高信息密度下的**低视觉噪声**；留白用于分组而非装饰；数字必须等宽防抖 |
| 白天随手记录 | Android，单手，30 秒 | 掏出手机、记一句、勾一个、放回 | 单手拇指可达；主操作下沉；**不要求填完整表单** |
| 周期回顾 | Windows，周末，40 分钟 | 浏览一周记录、看趋势、写总结 | 长文阅读舒适度（行长、行高）；时间与序号的纵向对齐 |
| 打断后回到工作 | 双端 | 从通知点进来、想立刻知道"到哪了" | 状态一眼可辨；回到原上下文不丢滚动位置 |

**由此得出的三条硬约束**：① 桌面优先保证"长时间不累"，移动端优先保证"30 秒能完成"；② 留白承担分组职责，线条只做次要提示；③ 状态必须三重冗余（色 + 形 + 字），因为用户常在低光/移动/分心状态下扫读。

### 1.2 当前界面的实测问题（本次改版的靶子）

| # | 问题 | 证据 | 后果 |
| --- | --- | --- | --- |
| P1 | 桌面侧栏 **14 个平铺一级入口 + 2 个折叠组** | `workbench_shell.dart:783-845` `_navigationTree` | 每次找页面都是一次线性扫描；认知负荷随入口数线性上升 |
| P2 | 移动端底部 5 项中**没有「成长」**，而低频的「设置」占据主位 | `workbench_shell.dart:110-116` + `:465-491` | 移动端高频动作（习惯打卡、日记）需绕道「更多」，与"30 秒完成"冲突 |
| P3 | 正文行高 1.35–1.5，中文长文偏紧 | `app_theme.dart:439-449`；`bodyLarge` 与 `bodyMedium` 同为 16/1.5 | 中文阅读疲劳；两个令牌语义重叠，实现时易误用 |
| P4 | 冷绿画布 `#F2F7F3` + 翡翠绿 `#2F7D57`，对比强、偏"软件感" | `app_theme.dart:9,15` | 与"温柔有机"方向相反；长时间注视偏刺激 |
| P5 | `signal #C84F45` 与 `danger #B53D3A` 语义重复 | `app_theme.dart:19,23` | 两个红并存，实现者无法判断该用哪个。**且 `signal` 自身在 canvas 上仅 4.16:1、subtle 上 3.91:1，本来就不满足 4.5:1** |
| P6 | `AppSpacing.xxl` 与 `xxxl` 取值相同（均 32） | `app_theme.dart:99-100` | 标尺失效，无法表达更强的分组层级 |
| P7 | `SectionHeading` 旁带航迹装饰条 | `common.dart:529` | 装饰性元素占用注意力，却不编码信息 |
| P8 | 侧栏选中态同时使用「12% 底色 + 25% 描边 + 3px 竖条」三种手段 | `workbench_shell.dart:1247-1300` | 过度强调，反而削弱选中态的辨识速度。**且选中文字（primary 压在 12% 绿底上）仅 4.21:1，不达标** |

### 1.3 设计理念：**有机形态必须承载语义**

「温柔有机」不是加圆角、换绿色。它的纪律是：**所有有机形态都必须编码真实数据，否则删除**。

- 「年轮弧」只表达周期完成度与专注时长；
- 「藤线」只表达真实时间顺序与任务状态；
- 圆润曲线只用于**降低视觉攻击性**，不用于装饰留白区域。

这条纪律直接继承原 `MASTER.md`「不添加船舶、海浪、虚构坐标、装饰编号」的原则——**换的是形态语言，不是严谨度**。

---

## 2. 信息架构：从 14 个平铺入口收敛为 6 个域

### 2.1 域模型

```
柔壤 · 年轮
│
├─ 今日 Today ──────────────── 唯一默认落地页，只回答"现在做什么"
│
├─ 计划 Plan ───────────────── 把事项组织进时间
│   ├─ 全部任务
│   ├─ 收件箱        （快速捕获的落点）
│   ├─ 周视图
│   └─ 任务群
│
├─ 项目 Projects ───────────── 跨域容器，独立入口（使用频率高）
│   └─ 概览 / 任务 / 任务群 / 里程碑 / 笔记与回顾
│
├─ 记录 Record ─────────────── 沉淀与回看
│   ├─ 笔记
│   ├─ 日回顾（日记）
│   ├─ 周回顾
│   └─ 月回顾
│
├─ 执行 Execute ────────────── 进入状态、约束行为
│   ├─ 专注
│   ├─ 协议
│   └─ 自律 / 限制
│
├─ 成长 Growth ─────────────── 长期积累
│   ├─ 目标
│   ├─ 习惯
│   ├─ 行为（国策）
│   └─ 成长
│
└─ 系统层（常驻，不占导航位）
    ├─ 全局搜索            Ctrl+K
    ├─ 快速捕获            Ctrl+Shift+Space
    ├─ 设置
    └─ 同步状态            侧栏底 / 移动端状态条
```

### 2.2 导航行为规则（降低 P1）

1. **侧栏只默认展开当前域**（手风琴，同时最多展开一个）。可见决策项从 ~20 降到 **6 域 + ≤5 子项**。
2. 域标题**整行可点**：点击非当前域 = 展开该域 + 进入其首个子页；点击当前域 = 折叠/展开。
3. 子项缩进 20px，并使用比域标题**小一号字重**（域 w600 / 子项 w400）建立层级，不额外加装饰。
4. 深度上限 3 层（域 → 子页 → 页内 Tab），超过即用页内 Tab 而非第四层导航。
5. 键盘：`←/→` 折叠展开，`Enter/Space` 进入，`Tab` 顺序 = 视觉顺序。

### 2.3 移动端底部导航调整（解决 P2）

| | 现状 | 新方案 | 依据 |
| --- | --- | --- | --- |
| 1 | 今日 | 今日 | 不变 |
| 2 | 任务 | 计划 | 更贴近"组织进时间"的心智 |
| 3 | 回顾 | 记录 | 覆盖笔记 + 日/周/月回顾 |
| 4 | 行为 | **成长** | 习惯打卡、目标、成长是移动端高频；原「行为」并入「成长」域 |
| 5 | 设置 | **更多** | 设置属低频系统项，降级进 Sheet（含 项目 / 执行 / 行为 / 搜索 / 设置） |

> 依据：底部导航的心理上限是 5 项，且每项应对应**一个用户心智域**而非一个页面。设置是"系统项"，不应与"活动项"争夺主位。
> 落地位置：`workbench_shell.dart:110-116`（`_mobilePrimarySections`）与 `:465-491`（destinations）。
> 所有 `WorkbenchSection` 枚举值保持不变，仅重新归组——**不破坏路由与控制器**。

---

## 3. 视觉语言：题材与形态

### 3.1 题材

「柔壤」= 土壤与生长的触感：燕麦米底、苔绿行动色、陶土成果色。界面像在纸面上摊开的一页，而不是一块发光的屏幕。

「年轮」= 时间与积累的结构：**同心弧**表达周期，**藤线**表达顺序。

### 3.2 两个结构性元素（取代原 LogRail）

| 元素 | 用途 | 编码 | 状态表达（必须多重冗余） |
| --- | --- | --- | --- |
| **GrowthArc 年轮弧** | 专注计时、周期完成度、28 日矩阵 | 真实进度百分比 / 真实时长 | 轨道 `divider` → 进行中 `sprout` → 达成 `clay`；圆心放等宽数字 |
| **VineRail 藤线** | 任务顺序、时间轴、回顾条目 | 真实时间或顺序 | **未开始**＝空心芽点 30% sprout；**进行中**＝实心芽点 + 外环；**已结果**＝实心 `clay`；**断口**＝连线断开 6px + `signal` 缺口弧 |

**断口（gap）而非红点**：冲突与中断在有机语言里表现为"生长被打断"，这个形态比红点更易被扫读识别，且天然具备形状冗余（不依赖颜色）。

### 3.3 明确禁止

`BackdropFilter`、玻璃拟态、装饰渐变、船舶/海浪/坐标/装饰编号、Emoji 代替图标、持续环境动画、Hover 才能触发的关键操作、把整页包进浮动卡片、嵌套卡片墙。

---

## 4. 色彩令牌

### 4.1 浅色 · 新芽晨光

> 换代说明：本节起主行动色的语义名由 `moss`（苔绿）改为 `sprout`（新芽绿）。
> 上一代色值（`canvas #F7F4EC` / `moss #4A6E4C` …）见第 18.2 节的历史表。

| 令牌 | 值 | 用途 | 实测对比度（vs surface `#FFFFFF`） |
| --- | --- | --- | --- |
| `canvas` | `#F5FAF0` | 页面画布 · 晨露绿 | — |
| `surface` | `#FFFFFF` | 主工作面 · 纯白 | — |
| `raised` | `#FFFFFF` | 对话框 / 菜单 / 浮层（以 1px 边框 + 投影分层） | — |
| `subtle` | `#EFF8F0` | 输入框 / 次级填充 | — |
| `ink` | `#17301B` | 正文与标题 | **14.24:1** ✅ |
| `inkMuted` | `#5D7961` | 辅助说明、次要元数据 | **4.80:1** ✅ |
| `inkFaint` | `#849A88` | 禁用文字、纯装饰 | 3.02:1（装饰级，禁止承载信息） |
| `divider` | `#DFEBE1` | 柔化分组线 | 1.23:1（装饰级，见 4.4） |
| `borderStrong` | `#C4D8C9` | 输入框 / 控件边界 | 1.50:1（配合 `subtle` 填充共同构成边界） |
| `sprout` | `#3E7F4B` | **主行动色**：主按钮、当前路线、链接 | **4.84:1** ✅ |
| `sproutContainer` | `#E2F3E5` | 选中底、tonal 按钮底 | — |
| `sproutOnContainer` | `#1D4A27` | 容器底上的文字 | 8.82:1 ✅ |
| `clay` | `#9B5227` | **成果色**：完成证据、里程碑、XP | **5.79:1** ✅ |
| `clayContainer` | `#F5E3D6` | 成果背景 | — |
| `clayOnContainer` | `#4A2410` | 容器底上的文字 | 10.86:1 ✅ |
| `signal` | `#9E3A32` | 冲突 / 危险 / 停止 | **6.76:1** ✅ |
| `signalContainer` | `#F7DEDA` | 危险底 | — |
| `signalOnContainer` | `#5A1F19` | 容器底上的文字 | 9.98:1 ✅ |
| `info` | `#3E6883` | 中性提示、同步状态 | **5.98:1** ✅ |
| `infoContainer` | `#DCE8F0` | 提示底 | — |
| `infoOnContainer` | `#1C3947` | 容器底上的文字 | 9.76:1 ✅ |
| `focusRing` | `= sprout`（实色） | 键盘焦点 2px 外环 | **4.56:1** ✅（需 ≥3:1） |

**主色为何不是原稿的 `#3F8F4F`**：方案 C 渲染稿的 sprout-600 在白底仅 **3.99:1**、
压绿画布 3.92:1，达不到正文级的 4.5:1 下限。故沿同一色相主轴压深，取**仍能达标的
最亮值** `#3E7F4B`（4.84:1 / 4.56:1）——这是「明快感」与「可读性」的边界点。

**分类色层未随换代调整**：8 类节点保持原色相，两两最小 ΔE00 仍为 **14.7**，
落在「可用」区间（见 12.4 的量尺）。唯一变动是 `outline` 由暖灰 `#918783`
换为绿灰 `#7E9284`——与新基座同温，压纯白 3.32:1，略高于 8 色地板（`olive` 3.30:1），
不改变底线。

### 4.2 深色 · 夜露

与浅色同源的暗绿姊妹版，取代原暖褐「夜间炭壤」。语义色与分类色层沿用浅色结论。

| 令牌 | 值 | 实测对比度（vs surface `#16231A`） |
| --- | --- | --- |
| `canvas` | `#0F1A13` | — |
| `surface` | `#16231A` | — |
| `raised` | `#1E2C21` | — |
| `subtle` | `#243528` | — |
| `ink` | `#E4EFE6` | **13.80:1** ✅ |
| `inkMuted` | `#A2B5A6` | **7.52:1** ✅ |
| `inkFaint` | `#73867A` | 4.20:1（装饰级） |
| `divider` | `#2A3A2F` | 1.35:1（装饰级） |
| `borderStrong` | `#3D5044` | 1.88:1 |
| `sprout` | `#7FCB8E` | **8.40:1** ✅ |
| `sproutContainer` | `#1D3A26` | — |
| `sproutOnContainer` | `#C9EBD0` | 9.66:1 ✅ |
| `clay` | `#E2A176` | **7.44:1** ✅ |
| `clayContainer` | `#4A3629` | — |
| `clayOnContainer` | `#F7DCC6` | 8.65:1 ✅ |
| `signal` | `#E88C7E` | **6.58:1** ✅ |
| `signalContainer` | `#4C2A26` | — |
| `signalOnContainer` | `#F9D9D3` | 9.54:1 ✅ |
| `info` | `#93B7CF` | **7.69:1** ✅ |
| `infoContainer` | `#27333D` | — |
| `infoOnContainer` | `#C7DCE8` | 9.11:1 ✅ |
| `focusRing` | `= sprout`（实色） | **9.20:1** ✅ |

### 4.3 语义纪律（硬规则）

1. `clay` **只用于成果与证据**，绝不用于错误、警告或危险。
2. `signal` **只用于冲突、危险、停止**，绝不用于成就或强调。
3. `sprout` 是唯一的**行动色**：每个工作面只允许一个 `sprout` 实心主按钮。
4. 容器底上的文字**永远使用对应的 `*OnContainer` 令牌**，不直接叠 `sprout`/`clay`/`signal`。
5. 颜色**不得作为唯一状态提示**：必须同时具备形状/图标或文字冗余。
6. `inkFaint` 与 `divider` 是装饰级对比度，**禁止承载任何必须被读到的信息**。

### 4.4 关于柔化线条的说明

`divider` 对比度仅 1.2–1.4:1，这是**刻意的柔化取向**。因此本方案规定：

> **分组职责由留白承担，线条只做次要提示。** 组间留白 ≥ 组内留白的 2 倍（见 6.3），即使完全看不见线条，分组依然成立。

需要明确边界的交互控件（输入框、复选框）**不依赖单一线条**，而是"`subtle` 填充 + `borderStrong` 描边 + 文字标签"三重构成。键盘焦点则使用实色 `sprout` 2px 外环（≥3:1），确保 `WCAG 2.1 一 4.11` 达标。

---

## 5. 字体与排版

### 5.1 字体（**全部复用现有离线资产，不新增字库**）

| 角色 | 字体 | 文件 | 字重 |
| --- | --- | --- | --- |
| 展示 / 页名 | `LXGW WenKai GB` | `LXGWWenKaiGB-Medium.ttf` | 500 |
| 正文 / 控件 | `IBM Plex Sans SC` | `IBMPlexSansSC-{Regular,Medium,SemiBold}.otf` | 400 / 500 / 600 |
| 数字 / 参数 | `IBM Plex Mono` | `IBMPlexMono-Medium.ttf` | 500 |

> **工程决策**：霞鹜文楷本身即手写楷体质感，天然符合"温柔有机"，无需引入新字体。**APK 字体体积零增量**（当前 `assets/fonts/` 合计约 37 MB，子集化由 `tool/subset_fonts.py` 负责）。

### 5.2 字阶（相对现行的变化）

| 令牌 | 现行 | 新值 | 变化理由 |
| --- | --- | --- | --- |
| `displayLarge` | 28 / 1.30 | **30 / 1.32** | 页名更像"摊开的一页"，配合更大留白不显局促 |
| `headlineMedium` | 22 / 1.35 | **22 / 1.40** | — |
| `titleLarge` | 18 / 1.40 w600 | **18 / 1.50 w600** | 中文行距放宽 |
| `titleMedium` | 16 / 1.375 w600 | **16 / 1.45 w600** | — |
| `titleSmall` | 14 / 1.45 w600 | **14 / 1.50 w600** | — |
| `bodyLarge` | 16 / 1.50 | **16 / 1.65** | 阅读列专用，中文字符方块感强，行高需显著大于西文 |
| `bodyMedium` | 16 / 1.50 | **15 / 1.60** | **修正 P3**：与 `bodyLarge` 重叠，改为桌面界面正文 15px |
| `bodySmall` | 13 / 1.35 | **13 / 1.50** | 辅助文字同样需要呼吸 |
| `labelLarge` | 15 / 1.40 w600 | **15 / 1.45 w600** | — |
| `labelMedium` | 13 / 1.35 | **13 / 1.45** | — |
| `labelSmall` | 12 / 1.30 | **12 / 1.40** | — |
| 阅读列 | — | 16 / **1.80** | 长文稳定阅读列 |
| 移动正文 | — | ≥ **16** | 沿用现行硬约束 |

### 5.3 排版纪律

- 字间距统一 `0`，**不使用负字间距**（现行 `_bodyStyle` 对 <18px 已强制归零，保留该保护）。
- 页名 `LXGW WenKai GB` 500；章节 / 对话框 / 编辑器标题 `IBM Plex Sans SC` 600；正文 400。**不合成不存在的字重**。
- 数字一律 `IBM Plex Mono` 500 + `fontFeatures: [FontFeature.tabularFigures()]`。
- 中文长文**不得依赖截断传达关键信息**；必须完整可读。
- 阅读列最大宽度 **620px**（16px 下约 38 个中文字/行，落在中文舒适区间 25–40 字内），居中。
- 所有文本使用主题文本样式，配合 `MediaQuery.textScalerOf`。**不使用固定高度裁切放大后的文字**。

---

## 6. 形状、圆角与留白

### 6.1 圆角标尺（圆润有机）

| 令牌 | 现行 | 新值 | 用途 |
| --- | --- | --- | --- |
| `xs` | — | **8** | 小标签、复选框、色块 |
| `control` | 8 | **12** | 按钮、输入框、Chip、列表选中底 |
| `card` | 12 | **16** | 卡片、FAB、菜单 |
| `panel` | — | **20** | 主要工作面、证据块 |
| `dialog` | 16 | **24** | 对话框 |
| `sheetTop` | 16 | **28** | 底部弹层顶角 |
| `pill` | 999 | **999** | 短状态标签 |

**同心圆角规则（硬约束）**：
> 当内边距 `P` < 外层圆角 `R` 时，内层元素必须使用 `r = R − P`；当 `P ≥ R` 时，内层自由使用 `control`。
>
> 例：面板 `R=20`、内边距 `P=20` → 内层控件可用 12（因 P ≥ R，已视觉分离）。
> 面板 `R=20`、内边距 `P=8` → 内层必须用 12（20 − 8 = 12，恰好等于 `control`）。

### 6.2 间距标尺（修正 P6）

| 令牌 | 现值 | 新值 | 用途 |
| --- | --- | --- | --- |
| `xs` | 4 | **4** | 图标与文字 |
| `sm` | 8 | **8** | 组内元素 |
| `md` | 12 | **12** | 组内元素上限 |
| `lg` | 16 | **16** | 控件内边距 |
| `xl` | 24 | **24** | 组间 |
| `xxl` | 32 | **32** | 章节之间 |
| `xxxl` | **32（重复）** | **56** | 页面级大分区 |
| `pageCompact` | 16 | **20** | <768 页面横向边距 |
| `pageMedium` | 20 | **28** | 768–1199 |
| `pageWide` | 24 | **36** | ≥1200 |

### 6.3 留白即分组（本方案的核心手法）

> **组间留白 ≥ 组内留白的 2 倍。**

| 层级 | 组内 | 组间 | 比值 |
| --- | --- | --- | --- |
| 列表项之间 | 8 | — | — |
| 分组（一组列表行） | 8 | 24 | 3.0 |
| 面板之间 | 12 | 32 | 2.7 |
| 页面大分区 | 16 | 56 | 3.5 |

这条规则让**移除分隔线后结构依然清晰**，直接降低视觉噪声（P7、P8 的成因）。因此：
- `SectionHeading` **移除装饰航迹条**；
- 侧栏选中态**只保留圆角底 + 圆头竖条**（去掉 25% 描边），减少同时生效的强调手段。

### 6.4 密度

| 项 | 现行 | 新值 | 说明 |
| --- | --- | --- | --- |
| 桌面列表行高 | 44 | **48** | 每屏可见行数下降约 8%，换取扫读准确率 |
| 移动列表行高 | 52 | **60** | 仍满足 ≥48dp 触控目标（整行可点） |
| 桌面控件高 | 36 | **40** | — |
| 移动控件高 | 48 | **52** | — |
| 触控目标下限 | 48dp | **48dp（不变）** | 视觉圆角不改变命中区矩形 |
| 面板内边距 | 16 | **20**（桌面）/ **16**（移动） | — |

---

## 7. 响应式布局

断点**沿用现行三档**，不新增（`app_theme.dart:46-50`）：

| 档位 | 范围 | 形态 |
| --- | --- | --- |
| Compact | `<768` 或高 `<600` | 单列；底部 5 项导航；FAB 快速捕获；全屏编辑用 `Dialog.fullscreen` |
| Medium | `768–1199` | 侧栏折叠为 76px 图标索引；主区 + 证据侧轨保持最小可读宽度 |
| Expanded | `≥1200` | 侧栏 240（可收至 76）+ 主工作区 + 证据侧轨 248 |

页面横向边距按 6.2 的 `pageCompact/pageMedium/pageWide`。
**必须检查视口**：`375×812`、`412×915`、`768`、`1024`、`1200`、`1440`、`1536×864`。
硬性要求：无横向溢出、无重叠、无因字体加载产生的位移；移动底部导航为安全区预留内边距。

---

## 8. 主要页面布局

通用骨架（桌面 Expanded）：

```
┌──────────────┬─────────────────────────────────────────────┬──────────────────┐
│  侧栏 240    │  PageHeader                                 │  证据侧轨 248    │
│  域导航      │  ─────────────────────────────────────────  │  （可折叠）      │
│              │                                             │                  │
│  快速新增 ▸  │  主工作区（弹性，单列内容 max 620）          │  年轮弧 / 计数   │
│  全局搜索    │                                             │  待办 / 到期     │
│  ─────────   │                                             │                  │
│  6 域        │                                             │                  │
│  ─────────   │                                             │                  │
│  同步状态    │                                             │                  │
└──────────────┴─────────────────────────────────────────────┴──────────────────┘
   ↑ 固定          ↑ 滚动                                         ↑ 独立滚动
```

### 8.1 今日 Today

**目的**：只回答"现在做什么"。

桌面（≥1200）三段式纵向：
1. **今日承诺**（顶部焦点区）— 最多 3 条，每条为一张 `panel` 卡（R=20），行内是复选框 + 标题 + 等宽时间段。三条全部完成时该区**整体降级为 inkMuted 并附一行"今日承诺已全部完成"**，不再出现庆祝动效（庆祝仪式保留给周回顾）。
2. **时间藤线**（VineRail 横向）— 06:00–24:00 刻度，当前时间为 `sprout` 芽点，承诺块落在对应时段。冲突显示为**断口**。
3. **完成证据** — `clay` 年轮弧 + 等宽 XP 数字，只呈现可追溯的执行事实。

移动（<768）单列：日期 + 一行进度（`3 / 5 已完成`，等宽数字）→ 承诺卡列表（60px 行）→ 悬浮「开始专注」。**不显示时间藤线**（横向交互在拇指场景下命中率低），改为按时段分组的纵向列表。

### 8.2 计划 Plan

- 左：任务列表（分组标题 + 48px 行），分组之间 24px 留白，无分隔线。
- 右（≥1200）：**内联详情面板**，点击列表行就地展开，**不跳页、不弹窗**。仅长文本（笔记正文、日记）走编辑器对话框。
- 移动：列表全宽；点击行推入详情页（保留返回栈，`workbench_shell.dart` 的 `_mobileHistory` 已支持）。

### 8.3 项目 Projects

主从结构：左侧项目列表（可折叠分组）→ 主区页内 Tab（概览 / 任务 / 任务群 / 里程碑 / 笔记与回顾）。Tab 使用 `UnderlineTabIndicator` 2px `sprout`，选中项同时加粗（形状 + 字重双冗余）。

### 8.4 记录 Record

**稳定阅读列**：内容 max 620px 居中，16 / 1.80。顶部是日期 + 所属周期（等宽）。右侧证据侧轨显示该周期的年轮弧与关联条目。滚动条常显（长文定位需求）。

### 8.5 专注 Focus

**收束型工作面**——进入专注后界面元素数量应**单调减少**：
- 默认态：任务选择 + 时长选择（预设 25 / 45 / 90）。
- 运行态：仅保留 `GrowthArc` 大年轮（直径 280 桌面 / 200 移动）+ 圆心等宽倒计时 + 一行任务名 + 「结束」次按钮。**隐藏侧栏、隐藏底部导航**（`PopScope` 拦截返回需二次确认）。
- 结束时：年轮弧由 `sprout` 转为 `clay`，落一条真实记录。

### 8.6 成长 Growth

- **28 日矩阵**：单元格 R=8，8px 间距；空 = `subtle`，完成 = `sprout`，达成连续里程碑 = `clay`。**不使用颜色渐变**（渐变会造成"程度"的虚假暗示）。
- **目标树**：缩进 + 藤线连接，节点芽点形状表达状态（同 3.2）。
- 移动：矩阵横向可滑，但**当前日期列默认吸附可见**。

---

## 9. 关键组件规范

| 组件 | 对应实现 | 规范 |
| --- | --- | --- |
| **OrganicPanel** | `SolidPanel` (`widgets/solid_panel.dart`) | 实色 `surface`，1px `divider` 描边，R=20，内边距 20，**默认无阴影**；hover 抬升 2px + `raisedShadow`（blur 16 / offset 0,6）。禁止玻璃拟态与渐变。 |
| **PageHeader** | `common.dart:437` | 页名 `displayLarge`（LXGW 500）；下方 4px 接一行 meta（`bodySmall` + `inkMuted`）；**移除下边框**，用 24px 下留白分组。 |
| **SectionHeading** | `common.dart:529` | 18 / 1.50 w600；**移除装饰航迹条**；层级由 24px 上留白确立。右侧可放一个 ghost 文本按钮（如「全部」）。 |
| **VineRail** | `common.dart:628`（原 `LogRail`） | 芽点 10px 圆头；连线 1.5px 圆头 `divider`；四态见 3.2。**设置页等无序内容不得创建假藤线**。 |
| **GrowthArc** | 新增 | `strokeCap: round`，线宽 10（大）/ 6（小）；轨道 `divider`，进度 `sprout`，达成 `clay`；圆心等宽数字。`reduce-motion` 下不做扫弧动画，直接呈现终态。 |
| **TaskRow** | `widgets/task_row.dart` | 行高 48 / 60；复选框 22px R=8，`sprout` 实心勾；**完成态 = 勾选 + 标题转 `inkMuted`，不使用删除线**（删除线降低中文可读性）；右侧时间等宽右对齐。状态同时具备「勾选形状 + 文字 + 颜色」三重表达。 |
| **SoftButton** | `app_theme.dart:578-679` | 四型：`filled`(sprout 实心，白字) / `tonal`(sproutContainer 底 + sproutOnContainer 字) / `outline`(borderStrong 边 + sprout 字) / `ghost`(透明 + sprout 字)。高度 40 桌面 / 52 移动，R=12。**每个工作面只允许一个 `filled`**。 |
| **StatusPill** | `common.dart:918` | R=999，内边距 8×4，12 / 1.40 w600；底用 `*Container`，字用 `*OnContainer`。**必须含文字，颜色仅作辅助**。 |
| **NumericText** | `common.dart:960` | `IBM Plex Mono` 500 + tabular figures；与相邻文字间距 4。 |
| **SurfaceIcon** | `common.dart:16` | 32px 底 R=10 + 18px 描边图标；语义底用对应 `*Container`。 |
| **EmptyState** | `common.dart:790` | 上下各 56px 留白；一句说明（15 / 1.60 `inkMuted`）+ **一个** filled 主操作。禁止插画式装饰。 |
| **SkeletonBlock** | `common.dart:321` | 圆角 = 被替换元素的真实圆角；脉冲 1.6s；`reduce-motion` 下静态。异步 >300ms 必须出现。 |
| **TickDivider** | `common.dart:1011` | 保留，但改为**左右内缩 20px 的柔化刻度**（不贯穿满宽），形成呼吸断点。 |

---

## 10. 降低认知负荷的交互设计

以下 10 条为本方案的核心交互原则，每条给出**可验收的规则**与**落地位置**。

| # | 原则 | 规则 | 落地 |
| --- | --- | --- | --- |
| 1 | **单一主行动** | 每个工作面**只有一个** `sprout` 实心按钮，其余降为 tonal/outline/ghost。用户无需判断"该按哪个"。 | `app_theme.dart:578-679` |
| 2 | **默认值优先** | 快速捕获**只强制一个字段（标题）**，其余按上下文预填：在今日页 = 今天 + 待办；在笔记页 = 笔记类型。 | 已实现于 `workbench_shell.dart:731-743`，推广到全部入口 |
| 3 | **渐进披露** | 首屏只显示决策必需字段；深层字段折叠在「更多字段」（默认收起）。一次只暴露必要层级。 | `quick_capture_sheet.dart`、`record_editor_dialog.dart` |
| 4 | **就地编辑** | 列表行可直接改标题 / 状态 / 日期，**不跳页、不弹窗**。对话框只保留给长文本与危险操作。 | `task_row.dart`、`task_hierarchy.dart` |
| 5 | **稳定骨架** | 切页保留滚动位置与展开状态（`IndexedStack` 已具备）；数字用等宽防抖；骨架屏尺寸 = 真实内容尺寸。 | `workbench_shell.dart:499-511`、`common.dart:321-436` |
| 6 | **状态三重冗余** | 任一状态必须同时有「颜色 + 形状/图标 + 文字」，任何一维缺失仍可正确判断。 | 全文；验收见 12 |
| 7 | **导航收敛** | 底部导航 ≤5 项；导航深度 ≤3 层；第 3 层用页内 Tab 而非新导航层。 | 第 2 节 |
| 8 | **宽容可恢复** | 破坏性操作给 **8 秒撤销**（SnackBar + 撤销）而非二次确认弹窗；编辑草稿自动保存。 | `showWorkbenchSnackBar`、`record_editor_dialog.dart` |
| 9 | **减少决策点** | 一屏内筛选 / 排序控件 ≤3 个，其余收进「筛选」面板且**显示已启用数量**（如「筛选 · 2」）。 | `plan_page.dart`、`notes_page.dart` |
| 10 | **空态即引导** | 空态给出**唯一推荐动作**，而不是多个并列选项。 | `common.dart:790` |

### 10.1 与"温柔"相关的两条补充

- **不用红点催促**：所有待办提示使用 `info` 而非 `signal`。`signal` 只保留给真正的冲突与破坏性风险，避免界面长期处于"警告态"。
- **完成不奖励弹窗**：勾选完成只做 120ms 的芽点推进，不弹庆祝层。庆祝仪式仅在周回顾等"真实周期节点"出现。

---

## 11. 动效

时长沿用现行三级（`app_theme.dart:72-90`），**曲线改为有机缓动**：

| 场景 | 时长 | 曲线（Flutter） | 曲线（CSS） |
| --- | --- | --- | --- |
| 控件反馈（按压/悬停/开关/焦点） | 120ms | `Curves.easeOut` | `cubic-bezier(0.22, 1, 0.36, 1)` |
| 状态变化（淡入、尺寸、Snackbar、弹层入场） | 180ms | `Curves.easeOutCubic` | `cubic-bezier(0.22, 1, 0.36, 1)` |
| 元素退场 | 150ms | `Curves.easeInQuad` | `cubic-bezier(0.4, 0, 1, 1)` |
| 页面切换 | 220ms | `Curves.easeOutCubic` | 同状态变化 |

**纪律**：不使用持续环境动画、装饰性漂浮、过长过渡、仅 Hover 可用的关键操作。
Flutter 使用 `MediaQuery.disableAnimationsOf(context)`；Web 使用 `prefers-reduced-motion: reduce`，**静态终态必须完整可用**。

---

## 12. 可访问性验收

### 12.1 对比度（已实测）

全部正文级配对 **≥4.5:1**，焦点环与选中指示条 **≥3:1**，实测明细见第 4 节表格。
共核算 **17 组跨令牌配对 × 浅/深两套 = 34 组，零未达标项**；
另核算 8 类节点分类色两两色差，最小 **ΔE00 = 14.7（浅）/ 16.4（深）**。
以上均由 `tool/verify_colors.py` 复算可复现。

### 12.2 键盘与触控

- 焦点环：实色 `sprout` 2px，`outline-offset: 2px`（Flutter 用 `Border` 外扩 2px）；实测 4.56:1（浅）/ 9.20:1（深），满足 ≥3:1。
- 焦点顺序 = 视觉顺序；侧栏 `←/→` 折叠，`Enter/Space` 进入。
- Android 系统返回、底部导航、FAB、侧栏折叠全部可用。
- 触控目标 ≥48dp；视觉圆角不缩小命中区（命中区为矩形）。

### 12.3 文本缩放与动效

- 支持系统文本缩放到 **200%**，不得出现遮挡、截断或横向滚动。
- 所有容器使用 `IntrinsicHeight` / 自然高度，**禁止固定高度裁切文字**。
- `prefers-reduced-motion` / `disableAnimations` 下所有终态完整可用。

### 12.4 对比度核算方法

使用 WCAG 2.1 相对亮度公式：

```
L = 0.2126·R_lin + 0.7152·G_lin + 0.0722·B_lin
其中 c_lin = c/12.92                     (c ≤ 0.04045)
           ((c + 0.055)/1.055)^2.4       (c > 0.04045)
对比度 = (L_亮 + 0.05) / (L_暗 + 0.05)
```

---

## 13. 落地清单

### 13.1 令牌迁移对照表（旧 → 新）

`lib/core/theme/app_theme.dart`

| 旧令牌 | 旧值 | 新令牌 | 新值 | 备注 |
| --- | --- | --- | --- | --- |
| `lightCanvas` | `#F2F7F3` | `canvas` | `#F7F4EC` | 冷绿画布 → 燕麦米 |
| `lightSurface` | `#FBFDFB` | `surface` | `#FFFDF9` | 中性白 → 暖白 |
| `lightRaised` | `#FFFFFF` | `raised` | `#FFFFFF` | 不变 |
| `lightInk` | `#1B2B20` | `ink` | `#232A22` | 冷墨绿 → 暖墨 |
| `lightInkMuted` | `#617268` | `inkMuted` | `#636B5E` | 同步修正 subtl 上不达标问题 |
| `lightDivider` | `#D6E3D8` | `divider` | `#E3DCCF` | 冷绿线 → 暖沙线 |
| — | — | `inkFaint` | `#8A9083` | **新增**（禁用/装饰） |
| — | — | `borderStrong` | `#D2CFC5` | **新增**（控件边界） |
| `lightPrimary` | `#2F7D57` | `moss` | `#4A6E4C` | 翡翠绿 → 苔绿（降饱和、更沉稳） |
| `lightPrimaryContainer` | `#DDEFE4` | `mossContainer` | `#DCE7D8` | 同步降低饱和 |
| — | — | `mossOnContainer` | `#1E3320` | **新增**（容器上文字 10.63:1） |
| `lightSecondary` | `#4F7561` | — | — | **删除**（与 inkMuted 语义重叠） |
| `lightSecondaryContainer` | `#E9F3EC` | `subtle` | `#EFEAE0` | 合并为中性次级填充 |
| `lightReward` | `#B8862D` → **`#8A6220`** | `clay` | `#9B5227` | 黄铜 → 陶土；**已先调深**（原值作文字色仅 2.99:1，见 16 节） |
| `lightGold` / `darkGold` | 与 `lightReward` 逐字节相同 | — | — | **已删除**（重复常量） |
| `WorkbenchTokens.marker` | 与 `reward` 同值 | — | — | **已删除**（同义双名，见 16 节） |
| `lightRewardContainer` | `#F4EBD8` | `clayContainer` | `#F5E3D6` | — |
| — | — | `clayOnContainer` | `#4A2410` | **新增**（10.86:1） |
| `lightSignal` | `#C84F45` | `signal` | `#9E3A32` | 亮红 → 砖红 |
| `lightDanger` | `#B53D3A` | `signal` | `#9E3A32` | **合并**（原两红语义重复，P5） |
| — | — | `signalContainer` / `signalOnContainer` | `#F7DEDA` / `#5A1F19` | **新增** |
| `lightInfo` | `#3F7FA0` | `info` | `#3E6883` | 降饱和 |
| — | — | `infoContainer` / `infoOnContainer` | `#DCE8F0` / `#1C3947` | **新增** |
| 深色 9 个对应令牌 | — | 同名 | 见 4.2 | 同理替换 |

### 13.2 改动清单（精确到文件与行号）

| # | 文件 | 位置 | 改动 |
| --- | --- | --- | --- |
| 1 | `lib/core/theme/app_theme.dart` | `AppColors` 7–43 | 按 13.1 全量替换；新增 `inkFaint` / `borderStrong` / 各 `*OnContainer` |
| 2 | 同上 | `AppRadius` 53–61 | 新增 `xs=8` / `panel=20`；`card` 12→16、`dialog` 16→24、`sheetTop` 16→28 |
| 3 | 同上 | `AppSpacing` 94–114 | `xxxl` 32→**56**（已完成）；**新增 `bottomNavClearance = 132`**（已完成）；`pageCompact/Medium/Wide` → 20/28/36（待执行） |
| 4 | 同上 | `WorkbenchTokens` 109–220 | 扩展字段（`mossOnContainer` 等）与 `copyWith` / `lerp` 同步 |
| 5 | 同上 | `_build` 的 `textTheme` 410–466 | 按 5.2 更新行高与 `bodyMedium` 字号 |
| 6 | 同上 | `controlHeight` 468 | 36→40（桌面）/ 48→52（Android） |
| 7 | 同上 | `listTileTheme.minTileHeight` 699 | 44→48 / 48→60 |
| 8 | 同上 | `focusRing` 292, 346 | 半透明 → **实色 moss** |
| 9 | `lib/ui/widgets/common.dart` | `SectionHeading` 529 | 移除装饰航迹条，改 24px 上留白 |
| 10 | 同上 | `LogRail` 628 起 | 重命名 `VineRail`，四态与圆头化改造 |
| 11 | 同上 | `StatusPill` 918 / `NumericText` 960 | 按 9 节规范 |
| 12 | 同上 | `SkeletonBlock` 321 / `EmptyState` 790 | 圆角对齐被替换元素；留白 56 |
| 13 | `lib/ui/widgets/task_row.dart` | 全文件 | 行高 48/60；完成态去删除线 |
| 14 | `lib/ui/workbench_shell.dart` | `_mobilePrimarySections` 110–116 | 按 2.3 调整为 今日/计划/记录/成长/更多 |
| 15 | 同上 | destinations 465–491 | 同步图标与文案 |
| 16 | 同上 | `_navigationTree` 783–845 | 重构为 6 域树 + 6 个域首子页映射 |
| 17 | 同上 | `_leaf` 1247–1300 | 选中态去掉 25% 描边，保留圆角底 + 圆头竖条 |
| 18 | `design_preview/assets/tokens.css` | 全文件 | 色值同步（保证预览与实现同源） |
| 19 | `test/visual_golden_test.dart` + `test/goldens/` | 全量 | **必须重新生成 Golden**（令牌全量变更） |
| 20 | `MASTER.md` | 第 2–8 节 | 由本文件取代，或在文首标注"视觉章节以 `ORGANIC_UI_SPEC.md` 为准" |

### 13.3 执行顺序建议

1. 令牌层（#1–8）→ 跑 `flutter analyze`
2. 组件层（#9–13）→ 跑 `flutter test`，重新生成 Golden
3. 导航层（#14–17）→ 人工走查 7 档视口
4. 预览同步（#18）+ 文档收敛（#20）
5. 终检：`flutter analyze` + 完整 `flutter test` + 浅/深色双主题截图 + `reduce-motion` + 文本缩放 200%

---

## 14. 与现行规范的冲突与取舍

| 冲突点 | 现行（`MASTER.md`） | 本方案 | 取舍理由 |
| --- | --- | --- | --- |
| 面板圆角 | 12 | 20 | 「圆润有机」方向的直接体现；同心圆角规则保证嵌套不打架 |
| 间距标尺 | 4/8/12/16/24/32 | 增加 **56** | 原 `xxxl` 与 `xxl` 重复（P6），标尺实际只有 5 档，无法表达页面级分区 |
| 侧栏结构 | 平铺 + 2 折叠组 | **6 域手风琴** | P1：14 个平铺入口的线性扫描成本 |
| 移动底部导航 | 今日/任务/回顾/行为/设置 | 今日/计划/记录/成长/更多 | P2：高频的成长功能缺席，低频设置占主位 |
| 危险色与信号色 | `signal` + `danger` 双红 | 合并为单一 `signal` | P5：语义重复导致实现歧义 |
| `SectionHeading` 航迹条 | 保留 | **移除** | P7：装饰性元素不编码信息 |
| 侧栏选中态 | 底 + 描边 + 竖条 | 底 + 竖条 | P8：同时生效的强调手段过多 |
| 列表行高 | 44 / 52 | 48 / 60 | 充足留白方向；触控下限 48dp 不变 |

**不改变**：全部 `WorkbenchSection` 枚举、控制器调用、数据模型、路由语义、本地优先存储策略、离线可用性、`SolidPanel` 实色体系（不引入玻璃拟态）。

---

## 15. 首批缺陷修复记录（已执行 · 2026-09-18）

本节记录 1.2 节中 **5 项不依赖视觉方向决策**的缺陷修复。改动只涉及语义与结构，
**未变更整体配色方向**（P4 冷绿画布 → 燕麦米仍待后续批次），因此现有冷绿体系保持完整、无半套配色。

| # | 问题 | 改动 | 实测证据 |
| --- | --- | --- | --- |
| 1 | 焦点环半透明不达标 | `app_theme.dart` `focusRing` 改实色 `AppColors.lightPrimary` / `darkPrimary` | 浅 1.54:1 → **4.90:1**；深 2.45:1 → **8.10:1**（要求 ≥3:1） |
| 2 | `bodyLarge` / `bodyMedium` 同为 16/1.5 | `bodyLarge` → 16/**1.65**（长文阅读列）；`bodyMedium` → 桌面 **15**/1.6，Android 保留 16/1.6 | 两档语义分离，不再重叠 |
| 3 | `signal` 与 `danger` 双红重复 | 删除 `WorkbenchTokens.danger`（**零引用死令牌**）与 `AppColors.*Danger`；`ColorScheme.error` 改指向 `signal` | 全库仅剩唯一红 |
| 4 | `AppSpacing.xxl` 与 `xxxl` 同为 32 | `xxxl` → **56** | 标尺恢复 7 档。两个令牌原本**零引用**，零布局风险 |
| 5 | 侧栏选中态三重强调 | 去掉 25% 描边；底色改 `primaryContainer`；文字改 `onPrimaryContainer` | 选中文字 4.21:1 → **10.32:1** |

### 15.1 修复过程中新发现的两个缺陷

原清单未列出，但在实算中被暴露，已一并修复：

| 问题 | 证据 | 处置 |
| --- | --- | --- |
| 浅色 `signal #C84F45` 自身不达标 | canvas 4.16:1 / surface 4.41:1 / subtle 3.91:1，均 < 4.5:1 | 合并时取达标值 `#B53D3A`（最差 4.93:1） |
| 深色 `signal #F07A6F` 在 `subtle` 上不达标 | 4.37:1 | 微调为 `#F2857A`（最差 4.76:1） |

> **教训：「合并重复令牌」必须先核对被合并值的达标情况。**
> 若当初直接保留原 `signal`，就会把一个不达标的色固化成系统唯一红——
> 表面上是"去重"，实际上是把缺陷升级成了规范。

### 15.2 验收状态

- `flutter analyze`：**No issues found**
- `flutter test`：**241 项全部通过**
- `test/goldens/`（12 张）与 `docs/images/`（9 张）已按新令牌重新生成

### 15.3 仍待执行（依赖视觉方向决策）

第 13.2 清单中其余各项（#1 全量换色、#2 圆角标尺、#6–#7 密度、#9–#13 组件层、#14–#17 导航层）**尚未执行**，
需在确认「冷绿 → 燕麦米」的整体换色后按 13.3 的顺序推进。（#3、#8、#17 的一部分已在 15、16 两节完成。）

---

**设计系统**：柔壤 · 年轮 v1.0
**日期**：2026-09-18
**状态**：可交付工程实现（令牌全部实测达标）
**配套可视预览**：`design_preview/organic/index.html`

---

## 16. 第二批缺陷修复记录（已执行 · 2026-09-18 晚）

本节处理 P5 的**同族问题**：`AppColors.lightReward` 与 `lightGold` 逐字节相同
（均 `#B8862D`，深色均 `#DDB65B`），导致 `WorkbenchTokens.reward` 与 `marker` 成为两个同值令牌。
同样未触碰配色方向，改动只涉及令牌去重、语义归位与达标修正。

### 16.1 核心结论：`reward` / `marker` 是「同义双名」，不是「异义同值」

先把 `marker` 的 **11 处**调用点的**真实语义**逐一读出，结果全部落在同一个语义族——「正向成果」：

| 调用点 | 语义 |
| --- | --- |
| `common.dart` `LogRailState.completed` | 已完成 |
| `growth_page.dart` 解锁印章边框/说明/底色 | 里程碑已解锁 |
| `task_row.dart` 完成节点脉冲 | 完成 |
| `focus_page.dart` 「灵息流转」状态圆点 | 正向运行中 |
| `ink_decoration.dart` 印章 Logo accent | 黄铜印章装饰 |
| `habits_page.dart` 「今日」格 2px 边框 | 位置指示 |
| `policies_page.dart` 节点类型分类色 | 分类 |
| `celebration.dart` 彩纸粒子 | 庆祝 |

这跟 `signal` / `danger` 的处境**完全同构**：两个名字、一个语义。因此正确解法与双红一致——
**合并为一个令牌，而不是发明新色相**（调色板里也没有第 5 个可用色相可分配）。

> **判据（可复用）**：两个令牌同值是不是缺陷，看**调用点语义能否区分**。
> 能区分 → 只是"碰巧同色"，可留；不能区分 → 是"同义双名"，必须合并。
> 本例属后者。反过来，这也是为什么不能简单地把 `marker` 的调用点统一替换成 `reward`——
> 下面 16.2 的四个缺陷全部出在"沿用旧值"会让缺陷固化。

### 16.2 沿用旧值会固化的四个真实缺陷（均已修）

| # | 问题 | 证据 | 处置 |
| --- | --- | --- | --- |
| 1 | `lightReward` 作**文字色**远不达标 | 画布 **2.99:1** / 面板 3.17:1 / `subtle` 2.81:1 / `rewardContainer` 2.73:1，**连 3:1 都不到** | 调深至 `#8A6220` → 5.03 / 5.34 / 4.73 / 4.60:1 |
| 2 | `habits_page` 今日边框与「已跳过」撞色 | 同格内 `WorkStatus.skipped => tokens.reward`，而今日边框 `tokens.marker`——同值 | 今日边框改 `tokens.info`（与格内三色 ΔE 40.4 / 44.3 / 69.0） |
| 3 | `celebration` 彩纸把同一颜色传了两次 | `colors: [primary, tertiary, marker, reward]` → 实际只有 3 色 | 改为 `[primary, tertiary, reward, info]`，四色互异 |
| 4 | `policies_page` 8 种节点类型只有 5 色 | `reward`/`ritual` 同黄铜；`habit`/`penalty`/`trigger` **三置红色**——「习惯」被标成危险色 | 按语义重新分组（见 16.3） |

### 16.3 节点类型配色：同色必须同语义

原实现的问题不是"颜色不够用"，而是**共享色与语义无关**。现按因果角色分五组，
共享色即共享分组；图标、简称与标签仍各自独立，颜色只作辅助线索（形状冗余，不依赖颜色）。

| 分组 | 类型 | 令牌 | 画布对比度 |
| --- | --- | --- | --- |
| 规则组（我要遵守的行为约束） | `policy`, `habit` | `primary` | 4.62:1 |
| 流程组 | `ritual` | `info` | 4.07:1 |
| 成果组（正向成果） | `goal`, `reward` | `reward` | 5.03:1 |
| 后果组 | `penalty` | `signal` | 5.25:1 |
| 触发组（由外部条件引发） | `trigger`, `reminder` | `mutedText` | 4.71:1 |

五色两两 ΔE 实测（**CIE76 口径**）均 ≥ 27。
⚠ 但换到 CIEDE2000 后，真实最弱对 `primary` ↔ `mutedText` 只有 **14.5**——
公式更正与统一后的结论见 17.2；本节数值请以 17.2 为准。

### 16.4 顺带修掉的两处「红色被当正向语义用」

`ColorScheme.tertiary` 与 `error` 同值（均为 `signal`），因此任何把 `tertiary`
当"第三个强调色"用的地方，实际渲染出来都是危险红：

| 位置 | 原状 | 处置 |
| --- | --- | --- |
| `task_row.dart` 「今日重点」星标 | `tertiary`（红），而 `notes_page` 的收藏星标用 `reward`（黄铜）——同一语义两套颜色 | 统一为 `tokens.reward` |
| `today_page.dart` `WorkStatus.rescheduled` 标签 | `tertiary`（红），与 `failed` 完全同色 | 改 `tokens.info` |

> **仍待决策**：`ColorScheme.tertiary == error` 是**色槽层面的重复**，与 P5 同类。
> 彻底解法是给 `tertiary` 一个独立值，但那会连带改动 `tertiaryContainer` /
> `onTertiary*` 与 6 处调用点，属于配色方向决策，故本轮只做归位、未改 `tertiary` 本身。

### 16.5 间距债实测（对应 P6 的后续部分）

对 `lib/` 全量 62 个 dart 文件实测：

| 指标 | 实测值 |
| --- | --- |
| `AppSpacing.*` 令牌使用次数 | 54 |
| `EdgeInsets` / `SizedBox` 硬编码次数 | 620 |
| ├ 落在标尺内（可直接映射） | 412 |
| └ **落在标尺外（无令牌可映射）** | **208（占硬编码 34%）** |
| **间距令牌化率** | **8.0%** |

标尺外数值呈现一条**与 4px 标尺无关的「2px 偏移子网格」**：`2`(16 次) / `6`(27) /
`10`(48) / `14`(25) / `18`(18)，合计 **134 处**。这说明这些间距并非随手写，
而是有意图的微调——因此**不建议直接把它们并进标尺**（补 10/14/18 会让标尺从 7 档膨胀到 10 档，
违背"标尺要短"的原则），应逐处判断归并还是保留。

**本轮只做了其中零风险的一类**——把有明确语义、却被硬编码 24 次的魔法数字收敛为令牌：

| 项 | 内容 |
| --- | --- |
| 新增令牌 | `AppSpacing.bottomNavClearance = 132` |
| 语义 | 移动端底部导航净空。由 `workbench_shell.dart` 底部 Column 推导：`NavigationBar 80 + 同步状态条 26 + 次级页面横幅约 29 ≈ 135`，取 132 作余量 |
| 替换范围 | **15 个文件、24 处**（21 处 `fromLTRB(…, 132)` + `restriction_page` 多行 + `settings_page` 三元 + `today_page` 叠加安全区 + `inbox_page`） |
| 附带修正 | `inbox_page.dart` 此前未导入主题文件，为使用该令牌补上导入 |

**改动时机有讲究**：`today_page.dart` 原本写的是 `132 + MediaQuery.paddingOf(context).bottom`——
硬编码值与安全区叠加。这类"魔法数字参与运算"的地方，一旦导航栏高度变化，
漏改一处就是内容被遮挡，因此是本次优先收敛的对象。

### 16.6 验收状态

| 项 | 结果 |
| --- | --- |
| `flutter analyze` | **No issues found** |
| `flutter test` | **241 项全部通过** |
| Golden 基线 | 配色改动后已重生成；随后 **`132` 令牌化在「不重生成基线」下复跑仍全绿**——即该重构**逐像素无变化**，这是零布局风险的直接证明 |
| 悬空引用 | `marker` / `lightGold` / `darkGold` 全库零残留（仅剩 `RestrictionProfile.markerPresent` 等无关同名词） |

### 16.7 方法论修正：对比度不能用来判断"两色是否可辨"

本轮一次自查中，我最初用**对比度**去验收"分类色是否互异"，得出
`secondary #4F7561` 与 `primary #2F7D57` 仅 1.04:1 的结论并据此判断它们"几乎同色"。
这个判据是**错的**：对比度衡量的是**亮度差**，适用于前景/背景可读性；
两个等亮度但不同色相的颜色，肉眼区分毫无问题。

正确判据是 **CIELAB 色差 ΔE**。改用 ΔE 后结论变了，也顺带实测出**第 8 个问题**：

| 令牌对 | 原记（CIE76） | 更正（CIEDE2000） | 判定 |
| --- | --- | --- | --- |
| `secondary` vs `primary` | 16.9 / 19.8 | **7.8 / 8.4** | 可辨但偏弱 ⚠ |
| `secondary` vs `mutedText` | 10.2 / 11.3 | **7.5 / 7.8** | 可辨但偏弱 ⚠ |
| `secondaryContainer` vs `primaryContainer` | — | **3.8** | 近乎同色 ✗ |

即 `lightSecondary #4F7561` 与 `primary`、`mutedText` 都只有 7–8 的色差——
作为**独立语义令牌**不足以承担区分职责。容器更明显：`lightSecondaryContainer
#E9F3EC` 与 `lightPrimaryContainer #DDEFE4` 只差 **3.8**。结合「零调用点」的事实，删除成立。

> ⚠ **本节数值原为 CIE76（ΔE\*ab）口径，已在 17.2 统一更正为 CIEDE2000。**
> 两套公式对同一对色的估值可差近 2 倍，**不可跨公式比较**。统一量尺见 17.2。

## 17. 第三批：节点配色独立化 · tertiary 归位 · 删除 lightSecondary（已执行 · 2026-09-18 深夜）

### 17.1 8 类节点各自独立：实际需要 3 个新色相，不是 2 个

| 项 | 值 |
| --- | --- |
| 原本可用于「分类」的色相 | **5**（绿 / 蓝 / 黄铜 / 红 / 灰） |
| 8 类各自独立所需 | **8** |
| 缺口 | **3** |

所以本轮补到**第 8 个**色相（青碧 / 橄榄 / 紫），而非第 6、7 个——
5 + 2 = 7 只能覆盖 7 类，必然还剩一对同色，「8 色各自独立」无法达成。

新增三色：

| 色相 | 浅色 | 深色 | 归属 |
| --- | --- | --- | --- |
| 青碧 teal | `#0F6668` | `#5FC0BD` | 习惯 habit |
| 橄榄 olive | `#5F6B1F` | `#B6C66C` | 目标 goal |
| 紫 violet | `#6B4A8C` | `#C2A6E4` | 触发器 trigger（兼 tertiary） |

分配原则由「同色必须同语义」升级为**邻近色同语义**：

| 语义组 | 类型 | 令牌 | 色相 |
| --- | --- | --- | --- |
| 规则 | policy 国策 | `scheme.primary` | 绿 151° |
| 规则 | habit 习惯 | `tokens.teal` | 青碧 ~175° |
| 流程 | ritual 仪式 | `tokens.info` | 蓝 200° |
| 成果 | goal 目标 | `tokens.olive` | 橄榄 ~70° |
| 成果 | reward 奖励 | `tokens.reward` | 黄铜 37° |
| 后果 | penalty 惩罚 | `tokens.signal` | 红 1° |
| 触发 | trigger 触发器 | `tokens.violet` | 紫 ~275° |
| 触发 | reminder 提醒 | `scheme.outline` | 中性 |

同组两色在色相环上相邻 → 关系暗示保留；每类各有独立色 → 可逐类指认。

**`reminder` 从 `mutedText` 改为 `scheme.outline` 是必要修正**：原 `mutedText #617268`
是 H=145° / S=8% 的**带绿味灰**，与 primary 绿 ΔE 仅 **14.5**——在绿色系里它天然贴着
主色。换成更亮更中性的 `outline #758781` 后该对 ΔE 升至 **17.5**，整体最小 ΔE 由
14.5 提升到 17.0。

### 17.2 实测结果 · 以及一次**跨公式比较的更正**

| 指标 | 浅色 | 深色 |
| --- | --- | --- |
| 8 色两两最小 ΔE（CIEDE2000） | **17.0** | **15.6** |
| 8 色两两最大 ΔE | 56.2 | 54.8 |
| accent 条在**面板**底色上的最低对比度 | 3.71:1 | 5.16:1 |

（accent 在 `subtle` 上更低：3.29 / 3.77；但 3px accent 条贴在 `panel` 上，以 panel 为准。）

**更正：第 16 节与本轮初稿用的不是同一个 ΔE 公式。** 初稿里「最小 ΔE 由上一轮的
≥27 降到 17.0」是**跨公式比较，无效**。核实：

| 配对 | CIE76 | CIEDE2000 |
| --- | --- | --- |
| `lightSecondary #4F7561` vs `primary #2F7D57` | 16.9 | **7.8** |
| `lightSecondary #4F7561` vs `mutedText #617268` | 10.2 | **7.5** |
| 5 色的最弱对 `primary ↔ mutedText` | 27.0 | **14.5** |

第 16 节记录的是 **CIE76（ΔE\*ab）**，本轮用的是 **CIEDE2000**。同一对色两套公式估值
可差近 2 倍，不可混用。统一到 CIEDE2000 重算后，结论**反转且对本方案有利**：

| 色板 | 浅色最小 ΔE | 深色最小 ΔE |
| --- | --- | --- |
| 5 色（上一轮分组） | 14.5 | 14.9 |
| 8 色（本轮独立） | **17.0** | **15.6** |

**独立成色后最小色差反而提高了。** 瓶颈从来不在「色相多不多」，而在
`primary 绿 ↔ mutedText`（H=145° / S=8% 的带绿味灰）只有 14.5——上一轮「五色两两
ΔE 均 ≥27」是 CIE76 口径，换到 CIEDE2000 后真实最弱对只有 **14.5**。本轮让
`reminder` 改用 `outline`，把这个瓶颈消掉了，于是 8 色的地板比 5 色还高。

最紧一对现为「国策绿 ↔ 习惯青碧」17.0：青碧必须落在绿(151°)与蓝(200°)之间，
取 ~175° 已是该区间最优。实测搜索 4×4×4×7 组候选，**17.0 就是这套色的上限**。

> **CIEDE2000 量尺（本规范统一采用，取代 16.7 的旧量尺）**：
> `< 2` 几乎不可辨（JND 量级） · `2–10` 可辨、但作分类色偏弱 ·
> `10–20` 分类色可用（3px 细条这类小面积元素应取该区间上沿） · `> 20` 差异显著。
>
> 8 色落在 17.0，处于「可用区间上沿」，正是细条元素需要的余量。颜色仍只作辅助线索：
> 每类另有独立图标与文案（`_typeIcon` / `_typeLabel`），形状冗余兜底。

> 已实测的两个档位：**8 色独立**（本轮，最小 ΔE00 17.0 / 15.6）与 **5 色分组**
> （上一轮，14.5 / 14.9）。7 色中间档**未实测，故不列**。

### 17.3 tertiary 独立值：从「等于 error 的红」到紫

原 `ColorScheme.tertiary == error == signal`（红）。后果是凡把 tertiary 当「第三个
强调色」用的地方，渲染出来全是危险红。现独立为紫，与 signal 的 ΔE 达 **31.0 / 29.1**。

| 槽位 | 浅色 | 深色 | 对比度（浅 / 深） |
| --- | --- | --- | --- |
| `tertiary` | `#6B4A8C` | `#C2A6E4` | vs panel 6.89 / 7.65 |
| `onTertiary` | `#FFFFFF` | `#2C1A40` | 7.04 / 7.43 |
| `tertiaryContainer` | `#EDE4F6` | `#3B2A4F` | — |
| `onTertiaryContainer` | `#3E2755` | `#E9DCF8` | 10.47 / 9.85 |

调用点实际是 **5 处**（非 6 处，已全库 grep 核实）：

| 位置 | 用途 | 处置 | 理由 |
| --- | --- | --- | --- |
| `focus_page.dart:99` | 「待确认启动」accent | **保留 tertiary**（现紫） | 中性的「待你处理」，非警示 |
| `celebration.dart:121` | 彩纸粒子 | **保留 tertiary** | 装饰性，紫正好补齐第 4 色 |
| `celebration.dart:272` | 默认 tint | **保留 tertiary** | 同上 |
| `restriction_page.dart:1373` | Android 平台提示 | → `tokens.info` | 是**信息**不是强调 |
| `calendar_page.dart:537` | 冲突标题文字 | → `scheme.onErrorContainer` | 是**冲突**，见下 |

**calendar 那处藏着一个真实缺陷**：冲突块底色是 `signal@12%` 淡红，原文字色
`onTertiaryContainer #6E241F` 只是因为 tertiary 恰好是红才「看起来对」。若只改
tertiary 值而不动这里，冲突标题会变紫——语义反而更错。而若改成 `tokens.signal`，
实测在 `signal@12%` 上仅 **4.43:1，不达标**。最终用 `onErrorContainer`：
**12.57:1 / 10.60:1**。

> 这是本项目**第二次**遇到「自色 12% 淡底上压自色文字不达标」——首次是侧栏选中态
> （P8，4.21:1）。可固化为一条通用检查项：凡在 `X@12%` 底上压 `X` 文字，必测。

### 17.4 删除 `lightSecondary`：ΔE 给出了硬证据

| 令牌对 | ΔE | 判定 |
| --- | --- | --- |
| `lightSecondary #4F7561` vs `primary #2F7D57` | **7.8** | 近乎同色 |
| `lightSecondary #4F7561` vs `mutedText #617268` | **7.5** | 近乎同色 |
| `darkSecondary #9BC4A8` vs `darkPrimary #76C893` | **8.4** | 近乎同色 |

三个值都落在「< 10 近乎同色」。处置：

- `AppColors` 删 4 个常量（`light/darkSecondary` + `light/darkSecondaryContainer`）
- `scheme.secondary` → `AppColors.lightPrimary` / `darkPrimary`
- `scheme.secondaryContainer` → `lightPrimaryContainer` / `darkPrimaryContainer`
- 唯一真实调用点 `common.dart:928`（`StatusPill` 默认底）→ `primaryContainer`

> `scheme.secondary` 本身**零调用点**（全库 grep 确认）；ColorScheme 要求槽位非空，
> 故指向 primary 族。这不是「留了冗余」，而是明确声明「本产品不使用独立次级色」。

### 17.5 测试与覆盖：本轮发现 golden 对色板零覆盖

新增回归守卫「节点类型 8 色各自独立」（断言纯函数映射，浅/深两套各 8 色两两不同），
并**验证过它确实会失败**——临时把 `habit` 改回与 `policy` 同色，测试立刻报出重复色值，
随后还原。

过程中发现两件事：

1. **golden 夹具只造了 3 种节点类型**（policy / reminder / ritual，且 ritual 那条被
   归档、不出现在界面上）→ 视觉基线实际只覆盖 **2/8** 类。已补齐 habit / ritual /
   goal / reward / penalty / trigger 六个节点，夹具现覆盖全部 8 类。
2. **靠截图守色板这条路不可靠**：节点色只体现在 3px accent 条上，落在视口外就是
   0 像素。像素取样实测：`guide_policies_tree`（1440×900）里 reward / penalty /
   trigger 三色命中 **0 像素**；改用界面渲染断言时也只找得到 1 种。故守卫测试改为
   直接断言纯函数 `rsipNodeTypeColor(tokens, scheme, type)`——为此把映射从
   `_typeColor(BuildContext, …)` 抽成不依赖 `BuildContext` 的公开纯函数。

### 17.6 验收状态

| 项 | 结果 |
| --- | --- |
| `flutter analyze` | **No issues found** |
| `flutter test` | **242 项全部通过**（较上轮 +1，即新增守卫） |
| Golden 基线 | 已重生成：`test/goldens/`（31 张，含 `ui_audit/` 19 张）+ `docs/images/`（9 张） |
| 悬空引用 | `lightSecondary` / `darkSecondary` / `SecondaryContainer` 全库零残留 |
| 像素反向核对 | 旧值 `#B8862D` / `#C84F45` / `#6E241F` 在基线中均命中 **0 像素** |

改动 **6 个文件**：`app_theme.dart` · `policies_page.dart` · `common.dart` ·
`calendar_page.dart` · `restriction_page.dart` · `test/visual_golden_test.dart`。

### 17.7 一处自我更正

上一轮曾写「国策与习惯同为绿色，代价是这两个**筛选 chip** 不能再靠颜色区分」——
**这句是错的**。核实：`policies_page` 的类型筛选 chip（第 314 行起）只渲染
`_typeIcon` 与 `_typeLabel`，**从未使用 `_typeColor`**；颜色在本页只出现在节点卡片的
左缘 accent 条上。所以那处「代价」并不存在，予以更正。

> 顺带一个未做的选项：可让筛选 chip 的图标染上类型色，使 8 色体系在筛选栏就能被
> 看到、可学习。本轮未做（属新增视觉元素，不在这三问范围内），列为备选。

---

## 18. 全面界面优化执行记录（已执行 · 2026-09-18 夜）

本节记录把第 1–14 节规范从「文档」落到「代码」的整批实施。**不改动任何产品语义**：
`WorkbenchSection` 枚举 31 个值、路由、`WorkbenchController` 的公开接口、数据模型与
真实文案均保持原样，只重排视觉层与信息架构层。

### 18.1 目标与范围

| 维度 | 内容 |
| --- | --- |
| 目标 | 让全部界面统一到「柔壤 · 年轮」；消除令牌层冗余与自相矛盾；提升可辨性与可访问性 |
| 范围 | 令牌层 → 组件层 → 应用层 → 导航层 → 预览站与文档，共 **9 个源码/测试文件 + 3 个预览文件** |
| 不在范围 | 业务逻辑、数据层、加密备份、平台专有能力（结束进程 / 改 hosts）、文案措辞 |
| 风格基准 | 4pt 基准 + 六档圆角 + 语义色板 4 族 + 分类色层 5 色；禁玻璃拟态、禁装饰渐变 |
| 响应式 | 三档断点 768 / 1200；页面边距 20 / 28 / 36；控件高 40 / 52；列表行 48 / 60 |
| 验证方式 | `flutter analyze` 零问题 + `flutter test` 全绿 + golden 重生成 + 三档视口浅/深走查 |

### 18.2 令牌层（`app_theme.dart`）

**语义色板整体换代**——原冷绿体系换成「晨间陶土 / 夜间炭壤」：

| 槽位 | 浅色 | 深色 | 说明 |
| --- | --- | --- | --- |
| canvas | `#F7F4EC` | `#171612` | 燕麦米画布，原为冷绿 `#F2F7F3` |
| primary · 苔绿 | `#4A6E4C` | `#93C08D` | 唯一行动色，5.70:1 |
| signal · 砖红 | `#9E3A32` | `#E88C7E` | 系统唯一红，6.66:1 |
| reward · 陶土 | `#9B5227` | `#E2A176` | 成果与证据，5.69:1 |
| info · 靛蓝 | `#3E6883` | `#93B7CF` | 中性提示，5.88:1 |

**三个此前已识别的缺陷在本轮真正落地**：

| 缺陷 | 处理 |
| --- | --- |
| `signal` 与 `danger` 双红并存 | 合并为单一 `signal`，`danger` 令牌删除 |
| `focusRing` 用半透明（实测 1.58:1 / 2.42:1，不满足 WCAG 1.4.11 的 3:1） | 改为实色 `primary`，达 **5.27:1 / 8.76:1** |
| `AppSpacing.xxl` 与 `xxxl` 同为 32，标尺实际只有 6 档 | `xxxl` 改为 **56**，恢复页面级分区表达能力 |

**新增容器槽令牌**：`*Container` / `*OnContainer` 成对出现（primary / signal / reward / info）。
这不是装饰性扩张——旧方案里「品牌色压在同色淡底」只有 4.49:1（陶土），
规范 4.3 因此明文要求改用 `OnContainer`（陶土槽位 **10.86:1**）。

**标尺整族调整**：

| 标尺 | 原值 | 现值 |
| --- | --- | --- |
| `AppRadius` | xs 未定义 / control 12 / card 12 / dialog 16 / sheetTop 16 | xs **8** / control 12 / card **16** / panel **20** / dialog **24** / sheetTop **28** |
| `AppSpacing` 页面边距 | compact 16 / medium 20 / wide 24 | compact **20** / medium **28** / wide **36** |
| 控件高度 | 44 / 48 | 桌面 **40** / Android **52** |
| 列表行高 | 44 / 52 | 桌面 **48** / Android **60** |
| 输入框 / 复选框边框 | `divider` | `borderStrong`（控件需要比分组线更明确的边界） |

### 18.3 组件层（`common.dart` 等）

| 组件 | 改动 | 依据 |
| --- | --- | --- |
| `PageHeader` | 去掉 1px 下边框，改 24px 下留白；页名升至 `displayLarge` 30px | 6.2 留白分组 |
| `SectionHeading` | 移除装饰航迹条，改 24px 上留白 + `titleLarge` 18px | 9 节 |
| `LogRail` → **`VineRail`** | 重命名 + 四态芽点用**形状**区分（空心 / 实心 / 双层同心圆 / 缺口弧 `CustomPaint`） | 12 节三重冗余 |
| `EmptyState` | 徽章由 `primary@8%` + `primary@40%` 描边改为 `primaryContainer` 底 + `onPrimaryContainer` 前景；上下 56px 留白 | 4.3 硬规则 |
| `StatusPill` | 底用 `*Container`、字用 `*OnContainer`，12/1.40 w600，内边距 8×4 | 4.3 + 5.2 |
| `SkeletonBlock` | 圆角改 `AppRadius.xs` | 6.1 同心规则 |
| `TickDivider` | 新增 `inset` 参数（左右内缩 20px） | 6.2 |
| `SolidPanel` | 默认圆角 `card` → `panel`（16 → 20） | 6.1 |
| `TaskRow` | 完成态**去掉删除线**（删除线会把 14px 中文横向切开）；行高 58 → 60 | 5.3 + 9 节 |
| `_VineBud` | 加 `ValueKey('vine-bud:<state>')`，使四态可被测试断言 | 12 节可测性 |

### 18.4 应用层（节点配色归位）

`policies_page.dart` 的 8 类节点映射中，**奖励节点 reward 从语义陶土色改挂分类色层琥珀
`tokens.amber`**。原因：新暖色基座上「陶土（语义成果色）」与「奖励节点」会互相挤到
ΔE00 12.4，语义色与分类色不应共用同一槽位。任务群中目标 goal 的橄榄色同时向黄绿偏移。

三行注释里的旧数值（原本写 17.0 / 15.6）已更正为实测的 **14.7 / 16.4**，并说明
「柔壤」降饱和与「分类色需高彩度」之间的固有取舍。

### 18.5 导航层（信息架构收敛）

`_navigationTree` 由「14 个平铺入口 + 2 个折叠组」重构为**6 域**：

| 域 | 子页 | 说明 |
| --- | --- | --- |
| 今日 | — | 一级入口 |
| 计划 | 全部任务 / 收件箱 / 周视图 / 任务群 | 手风琴 |
| 项目 | — | 一级入口（跨域容器且高频） |
| 记录 | 笔记 / 日回顾 / 周回顾 / 月回顾 | 手风琴 |
| 执行 | 专注 / 协议 / 自律 | 手风琴 |
| 成长 | 目标 / 习惯 / 行为 / 成长 | 手风琴 |
| 设置 | — | 系统层常驻，不占层级 |

- **手风琴规则**：`expanded = containsSelected && navigationGroupExpanded`——只默认展开
  当前域，同时至多一个。点非当前域标题 = 展开该域并进入其首个子页；方向键 → 同理。
- **移动底栏**由「今日 / 任务 / 回顾 / 行为 / 设置」改为「今日 / **计划** / **记录** / **成长** / **更多**」。
  原方案的缺陷是高频道「成长」缺席、低频「设置」占主位。
- **顺带消除一处冗余**：移动端 `AppBar` 曾另放一个 `tooltip: '更多'` 的图标按钮，
  与底栏末位「更多」调用**同一个** `_openMobileNavigation`。同一动作在同屏出现两个入口，
  且两个同名 tooltip 会让无障碍朗读与自动化定位都产生歧义，故移除——底栏末位是唯一入口。

### 18.6 预览站与文档同步

| 文件 | 改动 |
| --- | --- |
| `design_preview/assets/tokens.css` | 令牌区全量换代，与新色板同源；新增 `ink-faint` / `border-strong` / `teal` / `olive` / `violet` / `amber` / `outline` / `*OnContainer`；删除 `--danger` / `--gold` / `--marker` / `--secondary`；焦点环改实色；圆角与间距标尺同步；`--row-h` / `--control-h` 改为可切换变量 |
| `design_preview/*.html`（7 个文件） | 旧令牌引用批量迁移：`--danger→--signal`、`--gold→--amber`、`--marker→--reward`、`--secondary→--primary`、`pill gold→pill amber`（共 7 个文件、22 处） |
| `design_preview/index.html` | 色板区重写为 17 张卡（含分类色层与 onContainer 槽）；形状区 4 档 → 7 档；间距区补 xxxl 56 与新页面边距；强调元素区反映「分区条已移除」 |
| `design_preview/generate_glass_today.py` | 加废弃声明：其产物属玻璃拟态时期，与现行规范冲突，不再同步调色板 |
| `MASTER.md` | 头部加醒目声明：第 2–8 节视觉章节以本规范为准，仅第 1 节产品定位与流程性要求仍有效 |

> `design_preview/organic/index.html` 刻意**不改**——它是新旧对照文档，其中的旧色值
> 正是对照对象，改了反而失真。

### 18.7 实测数据（`tool/verify_colors.py` 可复现）

| 指标 | 浅色 | 深色 |
| --- | --- | --- |
| 关键配对未达标数 | **0** | **0** |
| 正文 / 工作面 | 14.50:1 | 14.39:1 |
| 辅助文字 / 工作面 | 5.45:1 | 6.96:1 |
| 苔绿 / 工作面 | 5.70:1 | 8.17:1 |
| 砖红 / 工作面 | 6.66:1 | 6.82:1 |
| 陶土 / 工作面 | 5.69:1 | 7.71:1 |
| 靛蓝 / 工作面 | 5.88:1 | 7.97:1 |
| 焦点环 / 画布（图形级，需 ≥3） | 5.27:1 | 8.76:1 |
| 8 色分类色两两最小 ΔE00 | **14.7** | **16.4** |
| 分类色贴面板底最低对比度 | 3.25:1 | 5.04:1 |

8 色的 ΔE 值为**可辨区间上沿而非宽裕**，这是「柔壤」降饱和与「8 类需独立色相」的
固有张力。可接受的前提是节点卡片同时有**图标与文字标签**兜底——颜色不是唯一线索
（规范 12 节）。最小那一对是 `habit 青碧` 与 `ritual 靛蓝`，两者色相本就相邻
（同属「规则/流程」邻近语义组），符合 17.1 的分配原则。

### 18.8 验收状态

| 项 | 结果 |
| --- | --- |
| `flutter analyze` | **No issues found** |
| `flutter test` | **全部通过**（242 项） |
| Golden 基线 | 已重生成：`test/goldens/`（含 `ui_audit/`）+ `docs/images/` |
| 悬空令牌 | `danger` / `marker` / `gold` / `lightSecondary` / `secondaryContainer` 全库零残留 |
| 旧色值像素反查 | 旧配色在重生成的基线中命中 0 像素 |

**两个非 golden 的真实失败已修复，而非放宽断言**：

1. `solid_panel_test` 原断言 `VineRail` 四态用四个不同**图标**表达——重命名后改为断言
   四个不同的**芽点结构**（形状方案），并把 `_VineBud` 的 Key 补上以支持断言。
2. `ui_coverage_regression_test` 有两项跟随 IA 变更更新：一级域 id `tasks → plan`；
   移动底栏标签改为五域新标签；并把 `find.byTooltip('更多')` 换成
   `descendant(of: NavigationBar)` 精确查找——原来的写法在短视口下会同时命中
   `AppBar` 与底栏两个同名按钮（正是 18.5 那处冗余的证据）。

### 18.9 遗留与备选

- **字号走查**：golden 覆盖 7 档视口，但字体缩放仅测 2.0x，未测 1.3x / 1.5x 等中间档。
- **360×800 与横屏**仍不在审计矩阵内（沿用旧缺口，本轮未扩展）。
- **备选（未做）**：让筛选 chip 的图标染上类型色，使 8 色体系在筛选栏即可学习（见 17 节末）。
- **备选（未做）**：`--row-h` 在预览站已是变量，但 Flutter 端仍有少量硬编码行高散落各处，
  可后续统一到令牌。

---

## 19. 配色换代：新芽晨光 / 夜露（已执行 · 2026-09-19）

把方案 C「新芽晨光」由视觉稿落成代码。**只换配色层，不动产品语义**：
`WorkbenchSection` 31 个值、路由、控制器接口、布局与间距标尺全部保持原样。

### 19.1 改动范围

| 文件 | 改动 |
| --- | --- |
| `lib/core/theme/app_theme.dart` | `AppColors` 浅/深两族整块换代；深色 `onPrimary`/`onSecondary` 随画布换轨；浅色投影色由暖褐转绿黑 |
| `lib/ui/widgets/common.dart` | `bestContrastingText` 两端默认值改为引用 `AppColors`，消除最后一处硬编码色 |
| `tool/verify_colors.py` | `LIGHT` / `DARK` 字典同步；配对标签 `苔绿` → `新芽绿` |
| `design_preview/assets/tokens.css` | 两族令牌逐值同步（33 处） |
| `tool/verify_token_parity.py` | **新增**：把「tokens.css 必须与 app_theme.dart 逐值对应」由人工纪律变成可执行判据（52 对） |
| `test/goldens/`、`docs/images/` | 视觉基线随新体系重生成 |

### 19.2 渲染稿 → 可交付色板的四处必要修正

视觉稿只保证好看，不保证达标。以下四点由实算得出，**不是对稿件的偏离，而是落地条件**：

| 项 | 稿件 | 落地 | 原因 |
| --- | --- | --- | --- |
| 主行动色 | `#3F8F4F` | `#3E7F4B` | 稿件值白底仅 3.99:1，低于正文级 4.5:1；取同色相最亮达标值 |
| 次级文字 | `#5E7A62` | `#5D7961` | 压绿画布 4.47:1，差 0.03 不达标 |
| 装饰级文字 | `#90A894` | `#849A88` | 工作面改纯白 `#FFFFFF` 后掉到 2.56:1 |
| 提醒节点色 | `#918783`（暖灰） | `#7E9284`（绿灰） | 与绿色基座同温；且须保住 8 色对比地板 |

### 19.3 未改动且刻意为之处

- **语义色三族（`signal` / `clay` / `info`）与 8 类节点色相原值保留**。它们在同一批
  新基座上复算仍达标，重调只会引入无收益的回归风险。
- **`raised` 与 `surface` 同为 `#FFFFFF`**，分层改由 1px `divider` 描边 + 投影承担——
  与稿件一致，也符合「面板用实色 + 中性边框」的既有禁令（禁玻璃拟态）。
- **未引入配色切换开关**。本轮是「换代」而非「并存」；如需保留「晨间陶土」作为可选
  主题，应另立任务做调色板注入改造，不在本轮范围。

### 19.4 验收

| 项 | 结果 |
| --- | --- |
| `flutter analyze` | 0 问题（仅 `tool/prelink_plugin_symlinks.dart` 一条既有 info） |
| `tool/verify_colors.py` | 浅 0 未达标 / 深 0 未达标 |
| 8 色最小 ΔE00 | 14.7（浅）/ 16.4（深），与换代前同档 |
| 分类色贴面板底最低对比度 | 3.30:1（浅）/ 5.29:1（深） |
| `tool/verify_token_parity.py` | 52 对逐值一致 |
| Golden 基线 | 已重生成 |

## 20. 组件形态落地：方案 C「新芽晨光」（已执行 · 2026-09-19 夜）

> 对比基准：`design_preview/green_schemes/scheme_c_sprout.html`（方案 C 源稿）
> 差异清单：`design_preview/green_schemes/SPEC_GAP_SCHEME_C.md`

### 20.1 为什么第 19 节不够

第 19 节换掉了 26 个色值，但方案 C 的辨识度**主要不在颜色**。当时的界面仍是
「一张 `LogSurface` 面板里塞多行、每行左侧 3px 状态色竖条、元信息拼成一行文本」——
这套样式语言方案 C 通篇没有使用。于是「颜色对了，但看起来不像」。本节落的是**形**。

### 20.2 令牌补充

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `AppRadius.dot` | 6 | 小方点（习惯打卡点） |
| `AppRadius.fab` | 19 | 快捷捕获 FAB |

新增色对 `oliveContainer` / `oliveOnContainer`（浅 `#F1F6DA` / `#5F6C15`，
深 `#2B3312` / `#D4D864`）——方案 C 的「今日必达」标签。**必须成对取用**。

其余方案 C 取值都落在既有标尺档位上：**不是改标尺，是把组件按标尺重搭。**

### 20.3 移动端今日页编排

顺序固定为：**概览卡 → 下一步行动 → 今日任务 → 今日重点 → 习惯打卡 → 更多今日记录（折叠）**。

- 概览卡与习惯打卡**必须落在顶层**：方案 C 里它们是手机 2 的视觉锚点，塞进折叠区整页就没有主角。
- 「今日（全量）」与「今日重点（聚光）」是**有意保留的双层结构**：源稿的
  `还剩 5 件事` / `今日重点 剩 3 项` 正说明后者是前者的子集，源稿只是没画全量列表。
  两层**共用同一种卡**，不再各自一套形态。

### 20.4 组件规格

| 组件 | 规格 |
| --- | --- |
| 移动页头（今日） | 日期小字 11.5px `inkFaint` letterSpacing .5 → 标题 20px w600 `display`；右侧 36×36 头像（`primaryContainer` 底、圆角 12、别名首字）。**无品牌块**，左边距 = 20 |
| 概览卡 | `subtle` 实色底（**不用渐变**）+ 1px `primaryContainer` 边框、圆角 20、内边距 16/14；标题 `还剩 N 件事 ✦` 15px w600 `onPrimaryContainer`；副行 `已完成 N 项 · 专注 Xm` 11.5px `primary` |
| 分段进度条 | **固定 12 段**，段高 5、段间距 5、圆角 3；已完成段 `primary`，其余 `primaryContainer` |
| 环形进度 | **直径 64**、环宽 7、圆头端点、自 -90° 起；轨道 `primaryContainer`、进度 `primary`；中心等宽百分比 |
| 任务卡 | **一项一张独立白卡**：`panel` 底、1px `panelBorder`、圆角 16、内边距 14/12/12、卡间距 7。**禁止**在卡内使用左侧状态色竖条与行间 `Divider` |
| 任务复选框 | **20×20 圆角 7** 方框、边框 1.8 `primaryContainer`；选中 = `primary` 实心 + 白勾 |
| 标签（pill） | 圆角 999、字号 10.5、内边距 2.5/8、w500。只用三族色：项目 → `primaryContainer`/`onPrimaryContainer`；今日必达 → `oliveContainer`/`oliveOnContainer`；时点 → `rewardContainer`/`rewardOnContainer`；逾期 → `signalContainer`/`signalOnContainer` |
| 分区标题 | 13.5px w600 标题 + 紧随的 11px `inkFaint` **纯文本**计数（`剩 N 项` / `已 2/4`）+ 右侧 `全部 →`（11.5px w600 `primary`）。**不用 chip 承载计数** |
| 习惯行卡 | 一项一张卡，左侧 **17×17 圆角 6** 方点（已打卡 = `primary` 实心），右侧 `连续 N 天` 用 `primary` 强调 |
| 移动底栏 | 高 58、`panel` 底、顶缘 1px `panelBorder`；选中 = 图标与文字转 `primary` + 文字下方 **16×2.5** 圆角短条。**不用 Material 胶囊 indicator** |
| 快捷捕获 FAB | **52×52、圆角 19**，由 `floatingActionButtonTheme` 下发 |
| 同步状态条 | **仅在 `cloudConfigured` 为真时出现**。方案 C 的 `pfoot` 只有 tabbar 一条 |
| 快捷捕获面板 | 输入卡（圆角 20、1.6px `primaryContainer` 边框、底部工具行 + 右下字数）→ `这是` chip 行 → `放到` 选择行卡（32×32 图标块 + 标题/副标题 + 右侧动作）→ 底部通栏 52px 主按钮 + 52×52 方形图标按钮 |

### 20.5 有意偏离（勿「顺手改回」）

| 项 | 偏离 | 理由 |
| --- | --- | --- |
| 概览卡底色 | 源稿 `linear-gradient(125deg, sprout-050, sprout-100)` → 实色 + 1px 边框 | 现行规范明文禁装饰渐变 |
| 桌面任务卡展开指示 | 保留右侧 `⌄` | 方案 C 只描述手机；桌面卡宽约为手机 1.8 倍，去掉后右侧大片空白且丢失可展开线索。**仅 `width >= 768` 出现** |
| 移动页头搜索图标 | 保留 | `ui_coverage_regression_test` 守卫「移动端今日可达全局搜索」，属功能要求 |
| 捕获页标题 / 主按钮 | `新增` / `收下` | 与产品内既有文案体系一致 |
| 承诺区专属动作 | 「专注此项」而非「开始专注」 | `开始专注` 是 `_NextStepPanel` 的主按钮文案；重名会让「今日只暴露一个下一步行动」的守卫失效 |

### 20.6 顺带修掉的缺陷

`TextButton.styleFrom(textStyle: ...)` 会**整体替换**主题下发的 textStyle，
其中不含 `fontFamily`，文字退回引擎默认字体。真机靠系统字体回退能兜住 CJK，
但 golden 环境没有系统 CJK，`全部 →` 一直渲染成豆腐块——基线在拍一张**错误的照片**。
已给 `common.dart` 与 `quick_capture_sheet.dart` 两处显式补 `fontFamily: AppFonts.body`。

### 20.7 验收

| 判据 | 结果 |
| --- | --- |
| `flutter analyze` | 0 问题 |
| `flutter test` | 243 通过 / 0 失败 |
| `tool/verify_colors.py` | 浅 0 / 深 0 未达标（未动色板） |
| 8 色最小 ΔE00 | 14.7（浅）/ 16.4（深） |
| 分类色贴面板底最低对比度 | 3.30:1（浅）/ 5.29:1（深） |
| `tool/verify_token_parity.py` | 56 对逐值一致 |
| 新增守卫 | `android_capture_sheet`（方案 C 手机 1 的形态基线） |
| Golden 基线 | 已重生成（`visual_golden_test` + `ui_audit_screenshot_test`） |

---

## 21. 桌面端适配：同一套组件语言的两栏落地（已执行 · 2026-09-19 夜）

> 承接 §20。§20 只动了移动端（`< 768`，方案 C 手机稿的编排）。
> 本节把**同一套组件语言**推到桌面（`>= 768`）。对照页：`design_preview/green_schemes/before_after/index.html`

### 21.1 边界：桌面不做「放大版手机」

桌面**保留**两栏信息架构（左：内容列；右 380px：今日重点 + 成长进度 + 承诺区）。
这是 §8.1 明文规定的桌面形态，而方案 C 只描述单列手机——它不构成对两栏的否证，
把桌面拍扁成单列是**过度适配**，不是「保持一致」。

本节要统一的是**跨端共享的那部分**：页头形状、分区标题、列表与卡片语言、
状态反馈、页脚的条件性。一句话判据：

> **同一段信息在两个端上应当是同一个组件，只是排布不同。**
> 凡「移动端已经改了、桌面还是旧形」的地方，就是本节要修的。

### 21.2 改动清单（14 项）

| # | 模块 / 功能点 | 文件 | 改前 | 改后 | 依据 |
| --- | --- | --- | --- | --- | --- |
| 1 | 页头 `PageHeader` | `widgets/common.dart` | 3px 主色竖条 + 12px 间距标记「这是页头」 | **删除竖条**；新增 `kicker` 参数（11.5px `inkFaint`，letterSpacing .5）承载引题 | §9 的 PageHeader 规格里没有这个形状；层级本就由字号与留白承担，且竖条把左内边距挤歪 15px |
| 2 | 今日页头日期位置 | `today_page.dart:52` | 日期走 `subtitle`（标题**下方**） | 日期改走 `kicker`（标题**上方**） | 与移动端 AppBar 的「日期 → 标题」同序，两端页头是同一段信息 |
| 3 | 分区标题 `SectionHeading` | `widgets/common.dart` | 桌面计数用 `subtle` 底 + 1px 边框的圆角 chip | 两端统一为淡色**纯文本**（`inkFaint`，桌面 12 / 移动 11） | 同一页里各分区的计数理应同形；§20.4 已定「不用 chip 承载计数」。统一后两端只剩字号差，与「控件按端取档」（行高 48/60、控件 40/52）同惯例 |
| 4 | 桌面侧栏品牌块 | `workbench_shell.dart` | 标题右侧挂 `IconButton`（收起导航），占走 94px → `个人工作台` 被截断成 `个人工作…` | 按钮**下移到页脚** `TextButton.icon` | 236 宽侧栏（减 padding 与 logo）需给约 100px 的标题留足 192px 才不省略 |
| 5 | 桌面侧栏页脚 | `workbench_shell.dart` | 同步状态行**恒显**；未配置同步时永远显示「本地数据已就绪」 | 改为 `if (controller.cloudConfigured)` | 与 §20.4 的移动端判据一致。未配置时该行每屏占一行、零信息量，`cloud_off` 图标已表达「本地模式」 |
| 6 | 桌面今日页内容列宽度 | `today_page.dart:455-500` | 内容铺满。1536 视口下主工作区约 847px，而任务卡可见内容只需约 300px → **65% 卡面是空白**，扫读要横穿整行 | `Center` + `ConstrainedBox(maxWidth: AppSpacing.contentMax)`（=720） | §8 要求主工作区单列内容有宽度上限：长文阅读列 620（中文 25–40 字/行），卡片列表 720 |
| 7 | 桌面今日页编排 | `today_page.dart:481` | 概览卡只在移动端出现 | 桌面左列**同样置顶** | 概览卡是方案 C 的视觉锚点；缺了它，同一天在两个端上是两种观感 |
| 8 | 标签（pill）密度 | `today_page.dart` × 8 处 | `dense: true` 恒真 | `dense: compact` | pill 高度应由端决定，与控件高度 Android 52 / 桌面 40 同一惯例 |
| 9 | 任务卡 / 习惯卡交互反馈 | `today_page.dart:1122` / `:1844` | 无 hover / focus 态 | `hoverColor: primary α.06`、`focusColor: primary α.1` | 桌面是指针端，缺指向反馈会让整卡「不像可点」 |
| 10 | 「今日任务」未开始态 | `today_page.dart` `_TodayTaskPreview` | `LogSurface` 面板 + `ListTile` + `Divider` | 一项一卡（`panel` 底 / 1px 边框 / R16 / 卡距 8）+ `NumericText` 序号 | §20.4 任务卡规格；§8.2 禁分隔线 |
| 11 | 卡片左侧状态色竖条 | `today_page.dart` `_ClosePanel` / `_CompactGrowth` | `LogSurface(accent: ...)` 的左侧 3px 色条 | **移除** | §20.4 明文禁「卡内左侧状态色竖条」 |
| 12 | 任务列表语言 | `plan_page.dart` / `projects_page.dart` | 裸行 + `Divider(height: 1)`；或「一张 Card 装多行 + 行间 `Divider`」 | **一行一卡** + 卡间 `SizedBox(height: 8)` | §8.2「分组之间留白，**无分隔线**」；§1「留白承担分组职责」。收件箱页与项目看板早已如此，此处补齐后全应用**只剩一种列表语言** |
| 13 | 页头下分隔线 | `behavior_page` / `calendar_page` / `inbox_page` / `projects_page` | `if (showHeader) const Divider()` | **删除** | §9 页头无下边框，留白负责分组 |
| 14 | 概览卡点缀字形 | `today_page.dart:2675` | `✦`（U+2726） | `Icon(Icons.auto_awesome, size: 13)` | 见 21.4——**这是一个真实缺陷，不是风格取舍** |

### 21.3 有意保留的桌面专属差异（勿「顺手改回」）

| 项 | 桌面 | 移动端 | 理由 |
| --- | --- | --- | --- |
| 信息架构 | 两栏：内容列 + 右 380px 固定栏 | 单列连续滚动 | §8.1 明文规定；方案 C 只是没画桌面 |
| 内容列宽度 | 限宽 720 并居中 | 满宽（页边距 20） | 桌面单行过长会丢失扫读锚点；`AppSpacing.contentMax` 的取值推导见 §8 |
| 任务卡展开指示 | 保留右侧 `⌄` | 无 | 见 §20.5：桌面卡宽约为手机 1.8 倍，去掉后右侧大片留白且丢失「可展开」线索 |
| 侧栏顶部标记 | `SealLogo` 品牌块 | 36×36 身份头像 → 设置 | 品牌块同时是启动页与「更多」面板的标记，三处同源；桌面已有品牌位，再挂头像就会出现两个身份入口 |
| 分区标题字号 | `titleLarge` | 13.5px / w600 | 桌面一屏信息量更大，需要更强的分区对比度来维持层级 |

### 21.4 顺带修掉的缺陷：`✦` 一直渲染成豆腐块

`_TodayOverviewCard` 标题结尾用 `✦`（U+2726）作点缀。该码位**不落在任何一份内置字体里**
（4 份文本字体的 cmap 全部查无）——所以它在真机与 golden 上都渲染成一个**豆腐块**。

这个缺陷在 `android_today` 与 `wide_shell_today_{light,dark}`、`wide_today_*` 里
**已经存在两轮**，只是没人盯着标题右侧那 13px 的位置看。改用
`Icon(Icons.auto_awesome, size: 13)`（U+E0B7，已确认在 `MaterialIcons` 内）。

新增常驻守卫 **`tool/verify_glyph_coverage.py`**：

- 扫描 `lib/**/*.dart` 中所有 `>= U+2000` 且不属 CJK / 标点跳过区的字符，与 5 份字体的 cmap 求交
- 同时复核关键字形（U+2726 应**缺席**，★ / ☆ / ✓ / • 应在场）与图标码位（U+E0B7 / U+E838 在场）
- 退出码：`0` 全覆盖 · `1` 有缺失 · `2` fontTools 缺席
- **改任何界面字面量后必跑**——工具依赖 fontTools，本机仅系统 Python 3.11 装有

### 21.5 验收

| 判据 | 结果 |
| --- | --- |
| `flutter analyze --no-pub` | 0 问题 |
| `flutter test` | **243 通过 / 0 失败** |
| `tool/verify_glyph_coverage.py` | 通过（无未覆盖码位） |
| `tool/verify_colors.py` | 未动色板，仍 0 未达标 |
| 重生成的 golden | 桌面 25 张（`ui_audit/*_desktop` 16 + `wide_*` 9）/ 移动 6 张（§20 + §21 累计） |
| 新增对照素材 | `before_after/old_wide_shell_{light,dark}.png`、`old_plan_desktop.png` |

---

## 22. 应用图标（已执行 · 2026-09-20）

不并入本规范——图标是**品牌资产**而非界面形态，涉及 ICO / VectorDrawable /
自适应图标等平台打包细节，独立成篇更清楚：

> **见 [`APP_ICON_SPEC.md`](APP_ICON_SPEC.md)** · 预览 `design_preview/icon/index.html` ·
> 生成器 `tool/build_app_icon.py`

与本规范的接口只有两条：**取色**与**圆角比**。

| 接口 | 取值 | 来源 |
|---|---|---|
| 天空 / 芽 / 土地 | `#3E7F4B` / `#FFFFFF` / `#9B5227`（深色档 `#16231A` / `#7FCB8E` / `#4A3629`） | §4 语义色层：新芽绿 = 生长、陶土 = 土地与成果 |
| 圆角砖 | `r = 232 / 1024 ≈ 0.22` | 与界内 `SealLogo` 同源 |

**同一轮顺带清掉的配色债**（均属上一代配色未清干净，不涉及界面代码）：
Android 启动链路 9 处取色统一到 `lightCanvas` / `darkCanvas` / `lightSurface`；
`ic_launcher_colors.xml` 的 `#2F7D57`（全仓最后一处废弃冷绿）随该文件一并删除。
`lib/` 与 `test/` 未改动，golden 无变化。
