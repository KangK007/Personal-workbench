# 个人航行日志 · 设计系统主规范

> 本文件是 Flutter 应用与 `design_preview/` 静态预览的共同设计源。任何新增页面、组件或状态都先对照本文件，再在对应实现中落地。产品语义、路由、控制器、数据模型和真实文案不因视觉调整而改变。

> [!IMPORTANT]
> **视觉规范主源已于 2026-09-18 变更为 [`ORGANIC_UI_SPEC.md`](ORGANIC_UI_SPEC.md)（代号「柔壤 · 年轮」）。**
> 本文件第 2–8 节（视觉方向、色彩令牌、字体排版、形状与留白、响应式、页面布局、组件视觉）
> 与新规范冲突时，**一律以 `ORGANIC_UI_SPEC.md` 为准**。这些章节保留是为了说明设计沿革，
> 不再具有约束力——其中 `LogRail` 已完成向 `VineRail` 的重命名，色彩、圆角、间距标尺
> 均已整族替换。
>
> 本文件**仍然有效**的部分：第 1 节产品定位与页面族划分、产品边界、路由与控制器约定，
> 以及实施清单中的流程性要求。
>
> 现行权威色值与标尺：`lib/core/theme/app_theme.dart`（`AppColors` / `WorkbenchTokens` /
> `AppRadius` / `AppSpacing`）与 `design_preview/assets/tokens.css`。两者同源，改一处必须改另一处。
>
> 另注：`design-system/default/MASTER.md` 仅为 `ui-ux-pro-max` 的检索基线，不具约束力；
> `UI_AUDIT_CODEX_OPTIMIZATION.md`（2026-08-19）结论已与代码脱节，**勿引用**。

## 1. 产品定位

### 目标用户

- 在 Windows 上进行完整规划、时间安排和周期回顾的单人科研/学习用户。
- 在 Android 上需要快速捕获、执行今日重点和进入专注状态的同一用户。

### 核心任务

收集事项，组织项目，确定今日重点，安排时间，进入专注，沉淀笔记，回顾执行证据。

### 内容特征

中文信息密集，时间和顺序明确，状态变化频繁，记录包含隐私信息，需要长时间阅读、稳定操作和可恢复的编辑流程。

### 页面族

| 页面族 | 页面与任务 | 主要布局 |
| --- | --- | --- |
| 工作台 | 今日、任务、计划、收件箱、日历、工作周 | 主工作区 + 证据侧轨 |
| 项目 | 项目概览、任务群、里程碑、笔记、回顾 | 主工作区 + 项目证据侧轨 |
| 记录 | 笔记、日志、回顾 | 稳定阅读列 + 事实快照/周期轨 |
| 执行 | 专注、自律、限制 | 计时工作面或高密度规则编辑 |
| 成长 | 目标、习惯、行为、国策、成长 | 树、28 日矩阵、协议状态、XP 证据 |
| 系统 | 设置、搜索、快速捕获、编辑器、附件、空态、加载、同步失败、回收站 | 就地操作，不制造虚假航迹 |

## 2. 视觉方向

### 概念

“个人航行日志”：用日志页、时间刻度、路径节点和证据索引组织界面。航迹只编码真实时间、任务顺序、里程碑、周期或证据，不添加船舶、海浪、虚构坐标、装饰编号或营销式 Hero。

### 标志性元素

- `LogRail`/日志航迹线：当前使用路线色，完成使用标记色，冲突/风险使用信号色，下一步使用中性节点。
- 页面标题使用克制的文楷字形；正文保持专业无衬线；时间、序号、XP 和参数使用等宽数字。
- 面板是实色工作面和细分隔线，不使用玻璃拟态、背景模糊、BackdropFilter、装饰渐变或嵌套卡片墙。

`ui-ux-pro-max` 的初始检索给出了 Soft UI/Roboto/Video-First Hero 组合。该结果适合营销型内容页，与本产品的隐私记录型工作台不匹配，因此本项目明确覆盖为以下原生工作台方向。

## 3. 颜色令牌

颜色表达层级和状态，不能作为唯一状态提示。每个完成、冲突、失败、选中状态必须同时有文字、图标、边缘条或结构变化。

### 浅色

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `canvas` | `#F2F7F3` | 清新浅绿页面画布 |
| `surface` | `#FBFDFB` | 主工作面、导航面 |
| `raised` | `#FFFFFF` | 对话框、菜单、浮层 |
| `ink` | `#1B2B20` | 正文和标题 |
| `ink-muted` | `#617268` | 辅助说明、次要元数据 |
| `divider` | `#D6E3D8` | 分隔线和边框 |
| `route` | `#2F7D57` | 主操作、当前路线、链接 |
| `route-container` | `#DDEFE4` | 选中和路线容器 |
| `signal` | `#C84F45` | 冲突、危险、停止 |
| `marker` | `#B8862D` | 完成、里程碑、奖励证据 |
| `marker-container` | `#F4EBD8` | 证据背景 |

### 深色

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `canvas` | `#0F1712` | 深色页面画布 |
| `surface` | `#16231B` | 主工作面、导航面 |
| `raised` | `#1D2D23` | 对话框、菜单、浮层 |
| `ink` | `#E8F2EA` | 正文和标题 |
| `ink-muted` | `#A5B8AA` | 辅助说明、次要元数据 |
| `divider` | `#31443A` | 分隔线和边框 |
| `route` | `#76C893` | 主操作、当前路线、链接 |
| `route-container` | `#21452F` | 选中和路线容器 |
| `signal` | `#F07A6F` | 冲突、危险、停止 |
| `marker` | `#DDB65B` | 完成、里程碑、奖励证据 |
| `marker-container` | `#4A3D22` | 证据背景 |

正文文字与背景至少达到 4.5:1，对大号文字和图形至少达到 3:1。禁用态可以降低对比度，但仍需保留可辨认结构和文字。

## 4. 字体与排版

字体全部从 `assets/fonts/` 离线加载，不使用运行时网络下载。

| 角色 | 字体 | 使用 |
| --- | --- | --- |
| 展示/页名 | `LXGW WenKai GB Medium` | 页名和少量章节标题 |
| 正文/控件 | `IBM Plex Sans SC` | 正文、表单、导航、按钮、说明 |
| 数字/参数 | `IBM Plex Mono Medium` | 时间、XP、序号、计时器、参数 |

- 字间距统一为 `0`，不使用负字间距。
- 页名使用 `LXGW WenKai GB Medium` 的 `500` 字重；章节、对话框和编辑器标题使用已离线加载的 `IBM Plex Sans SC SemiBold` `600`；正文、说明和列表内容保持 `400`，不合成不存在的字体字重。
- 桌面页名 24–28px，章节 17–22px，正文 15px，辅助 13px；移动页名 20px，正文不小于 16px。
- 行高：正文 1.5，稳定阅读列 1.7–1.8；中文长文不得依赖截断传达关键信息。
- 使用主题文本样式和 `MediaQuery` 文本缩放；不能用固定高度裁切放大后的文字。
- 数字列使用 `tabular-nums`/等宽字体，避免时间和计数变化造成布局抖动。

## 5. 形状、密度与层级

- 间距标尺只使用 `4/8/12/16/24/32px`。
- 面板圆角 `12px`，按钮/输入/Chip 圆角 `8px`，对话框和底部 Sheet 顶角 `16px`；短状态标签使用完整胶囊圆角。外层半径始终大于相邻内层控件，避免嵌套圆角互相争抢。
- 桌面列表行高约 `44px`；移动列表行高约 `52px`；所有触控目标至少 `48dp`（静态 Web 预览至少 `44px`）。
- 普通面板使用实色和 1px 中性边框，raised 浮层使用单一柔和阴影；不在同一层同时叠加宽阴影和描边。页面分区使用全宽带状布局，不把整个页面包进浮动卡片。
- 层级顺序：画布 → 工作面 → raised 浮层 → 对话框/Sheet。静态预览 z-index 只使用 `10/20/30/50/100`。

## 6. 响应式布局

断点保持现有约定：

- `<768px`：单列快速操作形态，隐藏桌面侧栏，顶部保留页名/搜索/更多，底部保留五项主导航，内容为真实时间或顺序航迹 + 就地操作。
- `768–1199px`：紧凑桌面，侧栏折叠为图标索引，主区与证据侧轨保持可读最小宽度。
- `>=1200px`：完整工作面，索引导航 + 主工作区 + 证据侧轨三栏；支持侧栏折叠。

必须检查 `375×812`、`412×915`、`768`、`1024`、`1200`、`1440` 和 `1536×864`。页面不能出现横向溢出、重叠或因字体加载产生的位移；移动底部导航要为安全区和内容预留底部内边距。

## 7. 组件与状态

- `SolidPanel`：所有主要工作面、证据块、表单区域的基础表面；禁止重新引入 `GlassSurface`、`GlassConfig` 或 `BackdropFilter`。
- `PageHeader` / `SectionHeading`：标题层级连续，标题旁的航迹条只表示真实页面上下文。
- `TaskRow`：完成、进行中、冲突、取消同时使用复选框/图标、文字和边缘结构表达。
- `StatusPill`：短标签只承载状态文字，文字必须完整可读；颜色仅作辅助。
- `NumericText`：时间、XP、序号、计时器和参数统一等宽数字。
- `SurfaceIcon`：列表行使用 36px 浅色语义底与 18px 描边图标；自律和设置工作面保留至少 16px 面板内边距，图标、开关和操作按钮不得贴边。
- `LogRail`：调用方只传现有记录的时间、顺序和状态；设置页等无序内容不创建假航迹。
- 空态、加载态、同步失败、回收站和危险确认沿用同一表面体系；异步操作超过约 300ms 必须反馈，提交期间按钮禁用并显示状态。
- 图标按钮必须有 `Tooltip`、可访问名称或可见文字；不要用 Emoji 代替图标。

## 8. 动效

- 控件反馈 `120ms`，状态变化 `180ms`，页面切换 `220ms`，使用 ease-out/ease-in。
- 不使用持续环境动画、装饰性漂浮、过长过渡或只在 Hover 上可用的关键操作。
- 任务完成时航迹节点推进；开始专注时工作面收束到计时器；动效服务于状态变化，不改变业务流程。
- Flutter 使用 `MediaQuery.disableAnimationsOf(context)`；Web 使用 `prefers-reduced-motion: reduce`，静态终态必须完整可用。

## 9. 可访问性验收

- 键盘导航保留可见 2px 焦点环，焦点顺序与阅读顺序一致；Android 返回、底部导航、FAB 和侧栏折叠继续可用。
- 交互控件有语义标签、选中/展开/禁用/错误状态；错误和同步失败不能只靠红色边框表达。
- 正文支持系统文本缩放，长文、表单、计时器和按钮在放大后不遮挡、不截断关键内容。
- 颜色对比度按本文件令牌检查；图标和节点要有文字、形状或结构冗余。
- 不产生横向滚动；弹层、菜单和底部导航不遮挡当前操作；支持 `SafeArea`。
- 测试覆盖：`flutter analyze`、完整 `flutter test`、Golden/截图、静态预览链接和减少动效模式。

## 10. 实施清单

- [x] Flutter 和静态预览共享颜色、字体、间距和圆角令牌。
- [x] 删除绿色玻璃表面及模糊依赖，保留 `SolidPanel`。
- [x] 保留全部 `WorkbenchSection` 路由、控制器调用、键盘操作和移动五项入口。
- [x] 覆盖工作台、项目、记录、执行、成长和系统页面族。
- [ ] 每次新增页面前先更新本文件或明确说明令牌例外。
- [ ] 发布前记录 Release APK 的字体体积增量，并完成人工 TalkBack/键盘检查。

## 11. 维护规则

根目录 `MASTER.md` 是项目级规范；`design-system/default/MASTER.md` 是 `ui-ux-pro-max` 生成的检索基线，仅作来源记录。若两者冲突，以本文件和现有产品边界为准。任何视觉修改都应同步 Flutter 主题、静态预览令牌和相关测试。
